# Deep audit of the manuscript - findings captured mid-run

Workflow `wf_24d7214c-a8a`. Nine lenses reported; verification was still running when this was written.
170 agents started, 154 returned.

Verifier votes are pooled per lens here, not matched to individual findings, because the run was
interrupted. Treat every finding below as UNVERIFIED and re-check it before acting.

To resume: `Workflow({scriptPath: "<script>", resumeFromRunId: "wf_24d7214c-a8a"})` - completed
agents return from cache, so only the unfinished verifiers re-run.

## bibliography  (8 raised; 8/23 skeptic votes refuted)

### [critical] paper/refs.bib line 5 (entry `luxemburk2024year22`); rendered as reference [1] in main.bbl line 24; cited 6 times in main.tex (lines 40, 42, 76, 99, 112, 443)
- **Paper says:** author  = {Luxemburk, Jan and Ple{\v{s}}n{\'\i}k, Karel and Hynek, Karel}  →  renders "J. Luxemburk, K. Plešník, and K. Hynek, \"CESNET-TLS-Year22: A year-spanning TLS network traffic dataset from backbone lines,\" Scientific Data, vol. 11, 2024."
- **Problem:** The author list of the dataset paper is wrong in three ways: (a) "Karel Plešník" is not an author of this paper at all; (b) the first author is Karel Hynek, not Jan Luxemburk; (c) three real authors (Jaroslav Pešek, Tomáš Čejka, Pavel Šiška) are omitted. This is the paper's central data source, cited six times, and the reference as printed names a person who did not write it. The file's own header comment (refs.bib line 1) asserts "Authors and titles verified against the arXiv API and Crossref, 22 Sep 2026", which this entry contradicts. The entry also omits the article number (11:1156).
- **Evidence:** WebFetch https://api.crossref.org/works/10.1038/s41597-024-03927-4 → "Authors: Karel Hynek, Jan Luxemburk, Jaroslav Pešek, Tomáš Čejka, Pavel Šiška; Journal: Scientific Data; Volume: 11; Issue: 1; Article Number: 1156; Published Year: 2024". Independently confirmed at WebFetch https://pmc.ncbi.nlm.nih.gov/articles/PMC11489426/ → "Authors (in order): 1. Karel Hynek 2. Jan Luxemburk 3. Jaroslav Pešek 4. Tomáš Čejka 5. Pavel Šiška … Volume: 11, Article Number: 1156, Year: 2024". No source lists a "Plešník".
- **Suggested fix:** Replace with: author = {Hynek, Karel and Luxemburk, Jan and Pe{\v{s}}ek, Jaroslav and {\v{C}}ejka, Tom{\'a}{\v{s}} and {\v{S}}i{\v{s}}ka, Pavel}, and add number = {1}, pages = {1156}. Note there is also an Author Correction (doi:10.1038/s41597-024-04055-9) you may wish to cite alongside. Then re-check the refs.bib header comment on line 1, since it claims a verification this entry did not receive.

### [major] paper/main.tex line 67 (Related Work, "Distillation in traffic classification")
- **Paper says:** "ResAware~\cite{resaware} is the closest in spirit. It reports that a website-fingerprinting student inherits some of its teacher's calibration and open-world behaviour, but on a single task, without a teacher-swap control and without a time axis."
- **Problem:** "without a time axis" is false, and it is the clause that does the most work in positioning this paper against its self-declared closest prior work. ResAware evaluates at temporal snapshots collected at 30-day intervals out to Day 150, and — most pointedly — tracks teacher–student agreement over that window using the Spearman rank correlation between student and teacher, which is structurally the same measurement as this paper's H1 statistic. A reader who checks the reference will find the differentiator overstated. (The other two differentiators, "single task" and "without a teacher-swap control", hold up, and the calibration claim checks out: ResAware reports ECE 0.138 → 0.034 at Day 150.)
- **Evidence:** pdftotext of https://arxiv.org/pdf/2606.17462 → line 491: "• Temporal Drift. Models are trained on the source domain and tested on temporal snapshots collected at 30-day intervals to evaluate cross-time generalization."  line 1078: "To quantify how much of the teacher's inter-class topology the student inherits, we track two metrics over the 150-day drift window. KL divergence … Spearman ρ measures the rank correlation between the student's and teacher's per-class similarity orderings…"  and "Var-CNN with ResAware … Spearman ρ increases steadily from 0.081 at Day 30 to 0.102 at Day 150."  Also line 489: "Data collection spanned November 2025 to April 2026…". Calibration confirmed at line 1077: "the baseline exhibits severe overconfidence (ECE = 0.138), whereas ResAware reduces ECE to 0.034".
- **Suggested fix:** Replace "without a teacher-swap control and without a time axis" with something accurate and still differentiating, e.g. "…but on a single task, without a teacher-swap control, and with a time axis that measures teacher–student agreement rather than whether the inherited property survives — its 150-day drift window tracks student-teacher Spearman correlation but not the student's standalone open-set quality against teacher-free controls." Alternatively: "…over five months rather than a year, and without a control that isolates the teacher's contribution from soft-label regularisation."

### [minor] paper/refs.bib line 111 (entry `functionalkd`); rendered in main.bbl line 66; cited in main.tex lines 44 and 70
- **Paper says:** author  = {Mason-Williams, Gabryel and Mason-Williams, Israel and Yannakoudakis, Helen}  →  renders "G. Mason-Williams, I. Mason-Williams, and H. Yannakoudakis"
- **Problem:** The first two authors are swapped. arXiv:2510.12615 lists Israel Mason-Williams first. Since the two share a surname, a reader cannot recover the correct order from "Mason-Williams et al." in the prose (main.tex line 70), so the bib entry is the only record of first authorship and it is wrong.
- **Evidence:** WebFetch http://export.arxiv.org/api/query?id_list=2510.12615 → "arXiv ID: 2510.12615v2, Title: 'A Functional Perspective on Knowledge Distillation in Neural Networks', Authors: Israel Mason-Williams, Gabryel Mason-Williams, Helen Yannakoudakis". (The substantive claims in main.tex line 70 do check out against the abstract: "Across 22 experimental setups, 9 architectures, and 7 datasets… knowledge distillation functions less as a robust compression-by-transfer mechanism and more as a data-dependent regulariser whose transfer component is biased towards negative asymmetric transfer.")
- **Suggested fix:** author = {Mason-Williams, Israel and Mason-Williams, Gabryel and Yannakoudakis, Helen}

### [minor] paper/refs.bib lines 43-48 (entry `netfound`); rendered in main.bbl line 33; cited in main.tex line 40
- **Paper says:** title = {{netFound}: Foundation model for network security}, author = {Guthula, Satyandra and Beltiukov, Roman and Battula, Navya and Guo, Wenbo and Gupta, Arpit}, journal = {arXiv preprint arXiv:2310.17025}, year = {2023}
- **Problem:** The title and author list do not match the record at arXiv:2310.17025 as it now stands. The current title is "netFound: Principled Design for Network Foundation Models" and the author list has grown to eight, with a different first author. A reader following the cited arXiv ID lands on a paper with a different title and byline than the reference gives. (Citing the v1 metadata is defensible, but then the entry should say so.)
- **Evidence:** WebFetch http://export.arxiv.org/api/query?id_list=2310.17025 → "Title: netFound: Principled Design for Network Foundation Models; Authors: Sylee Beltiukov, Satyandra Guthula, Haarika Manda, Jaber Daneshamooz, Wenbo Guo, Walter Willinger, Arpit Gupta, Inder Monga". WebFetch https://arxiv.org/abs/2310.17025 confirms the same current title/authors and a submission history of five versions ([v1] 25 Oct 2023 … [v5] 12 May 2026).
- **Suggested fix:** Update to the current title and author list, or keep the 2023 metadata and add note = {arXiv:2310.17025v1} so the version being cited is explicit. Since main.tex line 40 cites it only as an example of a large pre-trained "traffic foundation model", the current title actually fits the sentence better.

### [minor] paper/refs.bib lines 12-18 (entry `luxemburk2022reject`); rendered in main.bbl line 57 as "…in Computer Networks, 2023, arXiv:2202.11984."
- **Paper says:** @inproceedings{luxemburk2022reject, … booktitle = {Computer Networks}, year = {2023}, note = {arXiv:2202.11984}}
- **Problem:** Computer Networks is an Elsevier journal, not a conference. The @inproceedings type makes IEEEtran print "in Computer Networks", which presents a journal article as a proceedings paper, and the volume and article number are omitted. The entry is otherwise correct (authors, title, year, arXiv ID all match).
- **Evidence:** WebFetch http://export.arxiv.org/api/query?id_list=2202.11984 → "Title: 'Fine-grained TLS services classification with reject option'; Authors: Jan Luxemburk, Tomáš Čejka; Journal Reference: Computer Networks, vol. 220, p. 109467, Jan. 2023".
- **Suggested fix:** Change to @article with journal = {Computer Networks}, volume = {220}, pages = {109467}, year = {2023}, doi = {10.1016/j.comnet.2022.109467}.

### [nit] paper/refs.bib lines 130-135 (entry `teacherspet`); rendered in main.bbl line 84 as "…in Transactions on Machine Learning Research, 2022."
- **Paper says:** @inproceedings{teacherspet, … booktitle = {Transactions on Machine Learning Research}, year = {2022}}
- **Problem:** TMLR is a journal, so the @inproceedings type produces the wrong "in …" formatting. Authors, title and year are correct.
- **Evidence:** WebSearch "Lukasik Bhojanapalli Menon Kumar Teacher's pet TMLR" → published in Transactions on Machine Learning Research, 2022, by Lukasik, Michal; Bhojanapalli, Srinadh; Menon, Aditya Krishna; Kumar, Sanjiv; arXiv:2106.10494; OpenReview id ph3AYXpwEb.
- **Suggested fix:** Change to @article with journal = {Transactions on Machine Learning Research}, year = {2022}, note = {arXiv:2106.10494}.

### [nit] paper/refs.bib lines 160-166 (entry `whotaughtyou`); rendered in main.bbl line 99
- **Paper says:** booktitle = {Proc. ACL}, year = {2025}
- **Problem:** The paper appeared in Findings of ACL 2025, not the ACL main proceedings. Fields of the paper are otherwise correct.
- **Evidence:** WebFetch http://export.arxiv.org/api/query?id_list=2502.06659 → "Title: 'Who Taught You That? Tracing Teachers in Model Distillation'; Authors: Somin Wadhwa, Chantal Shaib, Silvio Amir, Byron C. Wallace; Comment: Findings of ACL 2025".
- **Suggested fix:** booktitle = {Findings of the Association for Computational Linguistics: ACL 2025}

### [nit] paper/refs.bib line 137 (entry `beyonddarkknowledge`); rendered in main.bbl line 87 as "J. Medina, P. Honeine…"
- **Paper says:** author = {Medina, Jose and Honeine, Paul and Bensrhair, Abdelaziz and Hadachi, Amnir}
- **Problem:** The first author's given name carries an accent (José) in the arXiv record; the entry drops it.
- **Evidence:** WebFetch http://export.arxiv.org/api/query?id_list=2606.12171 → "Authors: José Medina, Paul Honeine, Abdelaziz Bensrhair, Amnir Hadachi". (The substantive claim in main.tex line 70 checks out verbatim against the abstract: "calibration propagates from teacher to student independently of accuracy transfer, and temperature scaling governs a measurable accuracy-calibration trade-off".)
- **Suggested fix:** author = {Medina, Jos{\'e} and Honeine, Paul and Bensrhair, Abdelaziz and Hadachi, Amnir}

## claim-evidence  (16 raised; 0/0 skeptic votes refuted)

### [major] paper/main.tex line 55 (Contributions, bullet 2) and line 459 (Conclusion)
- **Paper says:** "Against the cheapest control (the same student trained directly and temperature-scaled), distillation improves no unknown-detection outcome we measured" ... and "A directly trained small model already has all three to the same degree"
- **Problem:** Both sentences are unqualified, and the paper's own Section V-A refutes them. Under the Mahalanobis score the conventional-temperature distilled student beats the directly trained student by +0.076 AUROC, and Section V-A line 370 states that directTS and direct are identical under both feature-space scores by construction — so the 'cheapest control' is beaten, on an unknown-detection outcome the paper itself measured and devotes a whole subsection to. The abstract carries the needed qualifier ("Under the logit-based scores nothing else transfers") and the Conclusion's own first sentence carries it ("only where a scoring rule can see it"), but the contribution bullet and the Conclusion's summary sentence drop it. A reader who reads only the contributions or the conclusion takes away the opposite of Section V-A.
- **Evidence:** python -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[(d.score=='maha')&(d.comparison.isin(['kdA4 - direct student','ls - direct student','teacherA - direct student']))][['comparison','estimate','ci_low','ci_high']])"  ->  kdA4 - direct student  0.076314 (0.069149, 0.083291); ls - direct student 0.087609; teacherA - direct student 0.072699.  No directTS row exists for maha/knnfeat (directTS == direct by construction).
- **Suggested fix:** Add the score qualifier to both: "...distillation improves no unknown-detection outcome we measured under the logit-based scores (Section V-A shows it does under a feature-space detector, where label smoothing does more still)"; and in the Conclusion, "A directly trained small model already has all three to the same degree under the logit scores; under a feature-space detector it is the label-smoothed student, not the distilled one, that leads."

### [major] paper/main.tex line 424 (Section V-G, kdF)
- **Paper says:** "Its closed-set accuracy on the validation week is $0.945$ against the directly trained student's $0.946$, and its macro-F1 $0.918$ against $0.919$"
- **Problem:** None of these four numbers is the quantity named. They are means taken across the `flows` grouping column, i.e. the average of the all-flows row and the overlapping >=5-packet row of the same validation week. The all-flows validation accuracy is 0.9407 (kdF) against 0.9410 (direct), and the all-flows macro-F1 is 0.9121 against 0.9131. The conclusion drawn (the arm is not degenerate) survives, but the reported values do not correspond to any evaluation set, and they contradict Section V-B, which gives the same reference model (direct, start 11, validation week) as 0.913.
- **Evidence:** python -c "import pandas as pd; d=pd.read_csv('results/track_a_exploratory/20260923-051258_S_train11-14/metrics.csv'); v=d[(d.split=='val')&(d.unknown=='all')]; print(v.groupby(['condition','flows'])[['accuracy','macro_f1']].mean().round(4))"  ->  kdF all 0.9407/0.9121, kdF ge5 0.9497/0.9229; mean of the two = 0.9452/0.9175 -> the paper's 0.945/0.918.  Same for direct (results/track_a/20260919-113607_S_train11-14/metrics.csv): all 0.9410/0.9131, ge5 0.9501/0.9244, mean 0.9456/0.9188 -> the paper's 0.946/0.919.
- **Suggested fix:** Replace with the all-flows validation values: "Its closed-set accuracy on the validation week is 0.941 against the directly trained student's 0.941, and its macro-F1 0.912 against 0.913."

### [minor] paper/main.tex line 383 (Section V-B, hardA)
- **Paper says:** "\kd{hardA} is otherwise an ordinary student, matching the directly trained student's closed-set accuracy on the validation week ($0.913$ against $0.913$)"
- **Problem:** 0.913 is macro-F1, not accuracy. hardA's validation accuracy is 0.9406 and direct's is 0.9410; their macro-F1s are 0.9125 and 0.9131. The comparison is sound but the metric is mislabelled, and combined with the Section V-G sentence the paper reports the same model's validation performance three different ways (0.913, 0.919, 0.946) all under the label "closed-set accuracy" or "macro-F1".
- **Evidence:** python -c "import pandas as pd; d=pd.read_csv('results/track_a_exploratory/20260922-130934_S_train11-14/metrics.csv'); print(d[(d.split=='val')&(d.flows=='all')&(d.unknown=='all')&(d.condition=='hardA')][['accuracy','macro_f1']])"  ->  0.9406 / 0.9125.  Direct (same week): 0.9410 / 0.9131.
- **Suggested fix:** Say "matching the directly trained student's closed-set macro-F1 on the validation week (0.913 against 0.913)", and use the same metric in Section V-G.

### [minor] paper/main.tex line 385 (Section V-B, Teacher C)
- **Paper says:** Teacher C is "trained on start date 11 only and within 0.4 macro-F1 points of Teacher A"
- **Problem:** The macro-F1 gap is 0.62 points, not within 0.4. The figure 0.4 is the *accuracy* gap (0.37 points). Teacher C is still comfortably inside the 2-point swap tolerance used for Teacher B, so nothing downstream changes, but the stated tolerance is wrong by 50%.
- **Evidence:** python -c "import pandas as pd; d=pd.read_csv('results/track_a_exploratory/20260923-032036_S_train11-14/metrics.csv'); v=d[(d.split=='val')&(d.flows=='all')&(d.unknown=='all')]; print(v[v.condition.isin(['teacherA','teacherC'])][['condition','accuracy','macro_f1']])"  ->  teacherA 0.973680 / 0.963854; teacherC 0.969945 / 0.957675.  macro-F1 gap = 0.0062 (0.62 pts); accuracy gap = 0.0037 (0.37 pts).
- **Suggested fix:** "within 0.7 macro-F1 points of Teacher A" (or "within 0.4 accuracy points"), whichever the author intended.

### [minor] paper/main.tex line 417 (Section V-E, Student Capacity)
- **Paper says:** "\kd{kdA4} is worse than the equally sized directly trained student at width 16 ($-0.054$ against \kd{direct}) and at width 96 ($-0.016$), under the energy score."
- **Problem:** The quoted numbers are not the equally-sized comparison. Table V's rows are each condition minus the *registered width-48* \kd{direct} student, as its caption states. Against the equally sized width-16 direct student the difference is about -0.030, not -0.054 (at width 96 it is about -0.017 against the quoted -0.016, so only the width-16 figure is badly off). The direction of the claim holds at both widths; the magnitude attached to it does not.
- **Evidence:** python -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); e=d[d.score=='energy'].set_index('comparison').estimate; print(e['kdA4_w16 - direct student']-e['direct_w16 - direct student'], e['kdA4_w96 - direct student']-e['direct_w96 - direct student'])"  ->  -0.029748 and -0.017328, against the -0.053771 and -0.015744 the paper quotes (both of which are against direct at width 48).
- **Suggested fix:** Either state the baseline explicitly ("kdA4 is worse than the registered-width direct student at width 16 (-0.054) and width 96 (-0.016)") or quote the like-for-like gaps, roughly -0.030 and -0.017, ideally as paired bootstrap differences.

### [minor] paper/main.tex line 410 (Section V-D, Dependence Between Start Dates)
- **Paper says:** "For the tuned \kd{kdA} arm they are $-0.00050$, $+0.00014$ and $-0.00152$, where the middle series is flat, which is why the pooled estimate is small. No start date shows the widening gap H3 predicted."
- **Problem:** The last sentence is contradicted by the number two clauses earlier. Start date 24's kdA slope is positive (+0.00014 AUROC/week) and its fitted gap rises from +0.0017 to +0.0058 across the series — i.e. that series does widen, slightly. The claim is the kind of universal negative the data does not support; "flat" and "no start date widens" cannot both be asserted about a positive slope.
- **Evidence:** python -c "import pandas as pd; print(pd.read_csv('results/exploratory/REVISION_FULL3/h3_per_start.csv'))"  ->  start 24, condition kdA, energy: slope_per_week = +0.000139, first = 0.001736, last = 0.005750 (the only positive slope of the six).
- **Suggested fix:** "Only one of the six series has a positive slope, and it is flat (+0.00014 per week); no start date shows widening of the size H3 predicted."

### [minor] paper/main.tex line 29 (abstract) and line 351 (Section V-A, first paragraph)
- **Paper says:** "where the energy score sees $0.001$" (abstract) and "Under those scores Teacher A and the directly trained student differ by 0.001 energy AUROC" (line 351)
- **Problem:** The measured Teacher A minus direct-student energy gap is 0.00039, which Section V-A's own results paragraph (line 362) correctly reports as "$0.000$ under the energy score ($-0.004$ to $0.004$)". The 0.001 figure belongs to a different comparison, Teacher A minus \kd{kdA} (0.00109, Table I's text). The paper therefore states the same quantity as both 0.001 and 0.000 within one subsection, and the abstract carries the wrong one into the headline 0.073-versus-energy contrast.
- **Evidence:** python -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[(d.score=='energy')&(d.comparison=='teacherA - direct student')])"  ->  estimate 0.000387.  Cross-check from per_window.csv: teacherA 0.835570 - direct 0.835192 = 0.000378.  Confirmatory components.csv, mean_gap_teacherA_minus_kdA_energy = 0.001091.
- **Suggested fix:** Use 0.000 in both places (abstract: "where the energy score sees 0.000"; line 351: "differ by 0.000 energy AUROC"), or say "0.001 over the tuned distilled student and 0.000 over the directly trained one".

### [minor] paper/exploratory_tables.tex line 6 (Table V caption; generated by scripts/15_exploratory_tables.py)
- **Paper says:** "Intervals are 95\% cluster bootstrap intervals over the 18 test windows, or over the nine of start date 11 for the conditions trained there alone (the width sweep, \kd{kdC} and \kd{kdF})."
- **Problem:** The caption is wrong for the Mahalanobis and feature-$k$-NN columns of \kd{kdM0}, \kd{kdM1} and \kd{hardA}. Those three conditions were trained at all three start dates (their energy and MSP entries do use 18 units), but their feature-space scores were only ever computed on start date 11, so those six entries rest on nine units. The caption names only the width sweep, kdC and kdF as nine-unit rows. This is the same class of error the deviation log records having fixed inside detection_advantage.csv; the caption was not updated to match.
- **Evidence:** python -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[d.comparison.str.startswith(('kdM0 -','kdM1 -','hardA -'))][['score','comparison','units_used']])"  ->  energy/msp rows: units_used = 18; maha/knnfeat rows: units_used = 9.  Confirmed by the source runs: results/feature_scores/20260923-090027_S_train11-14/metrics.csv contains only start-11 splits (test_w16-19 ... test_w48-51, val).
- **Suggested fix:** Change the generating script so the caption reads "...or over the nine of start date 11 for the conditions trained there alone (the width sweep, kdC and kdF) and for the feature-space scores of kdM0, kdM1 and hardA, which were computed on start date 11 only."

### [minor] paper/main.tex line 57 (Contributions, bullet 4) and line 308 (Section IV-C)
- **Paper says:** "distillation does not increase that reliance" and "Distillation does not increase shortcut reliance."
- **Problem:** This accepts a pre-registered null more firmly than the data allows. The H4a test pools $\rho=0.9$ and $\rho=1.0$, and the negative pooled estimate is produced entirely by $\rho=1$. At $\rho=0.9$ the KD student relies on the shortcut *more* than the direct student in every one of the three seeds (+0.0118, +0.0150, +0.0066; mean +0.0111). The paper hints at this ("a difference driven by $\rho=1$") and Table VI shows 0.196 against 0.185, but the headline sentence and the contribution bullet state a flat negative that one of the two tested reliability levels consistently contradicts.
- **Evidence:** python -c "import pandas as pd; d=pd.read_csv('results/shortcut/20260918-234156_S_train11-14/reliance.csv'); p=d.pivot_table(index=['rho','seed'],columns='model',values='reliance_f1_drop'); print((p['student_both_kd']-p['student_both_direct']).loc[[0.9,1.0]])"  ->  rho 0.9: +0.0118, +0.0150, +0.0066 (all three seeds positive); rho 1.0: -0.0677, -0.0849, -0.0722.  Pooled mean = -0.0319, matching H4a.
- **Suggested fix:** "Distillation does not increase shortcut reliance overall (H4a not supported); at $\rho=0.9$ the KD student relies on the feature slightly more than the direct student in all three seeds (+0.011), but the pooled test is dominated by $\rho=1$, where the direct student collapses almost completely."

### [minor] paper/main.tex line 108 (Section III-A, Time structure)
- **Paper says:** "This yields 9, 6 and 3 test windows, 18 in total, each a stratified sample of 100k known and 100k test-unknown flows"
- **Problem:** The last window is not. Start date 37's final window is a single week (w51-51) and contributes 100,000 known but only 43,579 test-unknown flows. "each" is false for one of the 18 units, and the shortfall is the reason the study's flow total is 3.54M rather than 3.60M.
- **Evidence:** grep 'test W-2022-51-51' results/track_a/20260919-143354_S_train37-40/log.txt  ->  "kept 143,579 of 143,579 flows (43,579 unknown)".  All other 17 windows: "kept 200,000 of 200,000 flows (100,000 unknown)".
- **Suggested fix:** "...each a stratified sample of 100k known and 100k test-unknown flows, except the final one-week window of start date 37, which has 43.6k test-unknown flows."

### [minor] docs/preregistration.md sections 3 and 9 vs paper/main.tex line 454 (Limitations)
- **Paper says:** "Novelty is simulated. No service first appears mid-year in CESNET-TLS-Year22, so unknown services are held out"
- **Problem:** The sentence is literally true under the paper's appearance criterion (all 180 services have flows from week 11 onward), but it omits something the pre-registration commits to. Section 9 of the frozen protocol lists "The natural-emergence services" as a planned exploratory analysis, and configs/splits.json reserves seven named services as a "Supplementary natural-emergence set" whose disable reason is "emerging after weeks 11-14 (reserved for the natural-emergence test)" — services that go from below the usability threshold to usable mid-year. The paper neither reports that analysis nor records it in the deviation log as dropped, while the limitation as worded implies the dataset offers no emergence to test. Other planned exploratory items that were dropped (natural shortcut fields) *are* explicitly acknowledged in the same paragraph, so the omission is inconsistent with the paper's own practice.
- **Evidence:** python -c "import json; s=json.load(open('configs/splits.json')); print(s['emerging_w11_14']); print(s['disabled_reasons']['amazon-alexa'])"  ->  ['amazon-alexa','cesnet-kalendar','cesnet-login','cesnet-perun','font-awesome','gitlab','redmine'] ; 'emerging after weeks 11-14 (reserved for the natural-emergence test)'.  docs/preregistration.md line 36 and line 186 list the same set as a planned exploratory analysis. No results file under results/ reports it.
- **Suggested fix:** Add one clause: "...so unknown services are held out. Seven services do cross a volume threshold mid-year and were reserved for a natural-emergence probe in the protocol; that probe was not run, and is left for future work." (or run it and report it).

### [nit] paper/main.tex line 183 (Section IV-A)
- **Paper says:** "The teachers are 5--6 macro-F1 points more accurate than any student"
- **Problem:** The universal quantifier fails at both ends of the range. Teacher B leads the best student (\kd{kdA}) by 4.81 points, below the stated floor; Teacher A leads \kd{kdB4} by 7.08 points, above the stated ceiling. The true spread over the six teacher-student pairs is 4.8 to 7.1 points.
- **Evidence:** python -c "import pandas as pd; p=pd.read_csv('results/analysis/CONFIRMATORY/per_window.csv'); d=p[(p.flows=='all')&(p.unknown=='all')]; g=d.groupby('condition').macro_f1_mean.mean(); import itertools; v=[(t,s,round(100*(g[t]-g[s]),2)) for t in ['teacherA','teacherB'] for s in ['direct','directTS','ls','kdA','kdB','kdA4','kdB4','enddA']]; print(min(v,key=lambda x:x[2]), max(v,key=lambda x:x[2]))"  ->  ('teacherB','kdA',4.81) and ('teacherA','kdB4',7.08).
- **Suggested fix:** "The teachers are 5 to 7 macro-F1 points more accurate than any student" (or "about 5 points more accurate than the best student").

### [nit] paper/main.tex line 449 (Discussion, Label smoothing)
- **Paper says:** "under feature $k$-NN within 0.001 of EnDD at the top, a margin we do not read as a ranking"
- **Problem:** The margin is 0.0014, not within 0.001. The point being made (the two are effectively tied) is unaffected, but the stated bound is false as written.
- **Evidence:** python -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); e=d[d.score=='knnfeat'].set_index('comparison').estimate; print(e['enddA - direct student']-e['ls - direct student'])"  ->  0.001402 (enddA +0.044039, ls +0.042637).
- **Suggested fix:** "within 0.002 of EnDD at the top".

### [nit] paper/main.tex lines 362 and 374 (Section V-A)
- **Paper says:** "$0.034$ better under the feature $k$-NN score ($0.028$ to $0.040$)" and "$-0.092$ against \kd{kdA}, CI $-0.100$ to $-0.085$"
- **Problem:** Two interval endpoints are rounded in the wrong direction. The feature-$k$-NN upper bound is 0.039454, which rounds to 0.039, not 0.040. The EnDD energy lower bound is -0.099485, which rounds to -0.099, not -0.100 — and Table II reports the same quantity as (-0.099, -0.084) from the confirmatory run, so the paper gives two different intervals for one estimate without saying they come from different bootstrap runs (1,000 resamples confirmatory, 300 exploratory).
- **Evidence:** python -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[((d.score=='knnfeat')&(d.comparison=='teacherA - direct student'))|((d.score=='energy')&(d.comparison=='enddA - kdA'))][['score','estimate','ci_low','ci_high','n_boot']])"  ->  knnfeat 0.033806 (0.028033, 0.039454), n_boot 300; energy -0.092009 (-0.099485, -0.084715), n_boot 300.  Confirmatory components.csv for the same H5 energy estimate: (-0.099237, -0.084368), n_boot 1000.
- **Suggested fix:** Print 0.028 to 0.039 and -0.099 to -0.085; and for the EnDD row cite the confirmatory interval from Table II, or say in one clause that the exploratory intervals use 300 resamples.

### [nit] paper/main.tex line 233 (Section IV, reading conventions)
- **Paper says:** "With 1,000 resamples the smallest attainable one-sided $p$ is $1/1001 = 0.000999$, so $0.009$ is the smallest Holm-corrected value a family of ten can produce"
- **Problem:** Not true as stated. Holm multiplies the rank-$k$ $p$-value by $(11-k)$, so the floor depends on rank: a bootstrap hypothesis at rank 1 would get $10\times0.000999 = 0.00999$, and one at rank 3 could get $8\times0.000999 = 0.00799$. H1 receives 0.008991 only because it sits at rank 2 behind H4b1, whose $p$ comes from a $t$-test and is not subject to the bootstrap floor.
- **Evidence:** python -c "import pandas as pd; print(pd.read_csv('results/analysis/CONFIRMATORY/hypotheses.csv'))"  ->  H4b1 p_iut 0.000473 -> holm 0.004731 (x10); H1 p_iut 0.000999 -> holm 0.008991 (x9).  Re-running Holm on the ten IUT p-values reproduces both exactly.
- **Suggested fix:** "...so 0.009 is essentially the resolution floor for a bootstrap-based hypothesis in this family, and rows shown as 1.0 are bounded rather than precisely measured."

### [nit] paper/main.tex line 353 (Section V-A)
- **Paper says:** "We re-scored every model already trained using two detectors built on its penultimate features"
- **Problem:** Six of the conditions in the very table this sentence introduces carry dashes in the Mahalanobis and feature-$k$-NN columns, which the caption glosses as "a score that was not computed": the four width-sweep rows and \kd{kdC}. "every model already trained" overstates the coverage against the table on the same page.
- **Evidence:** Table V (paper/exploratory_tables.tex lines 14-22) shows '---' for direct_w16, direct_w96, directTS_w16, directTS_w96, kdA4_w16, kdA4_w96 and kdC.  Confirmed in the data: python -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(sorted(set(d[d.score=='energy'].comparison)-set(d[d.score=='maha'].comparison)))"  ->  the six width-sweep rows plus 'kdC - direct student'.
- **Suggested fix:** "We re-scored the teachers and the pre-registered students, and later the exploratory teacher-swap arms, using two detectors built on their penultimate features."

## figures-tables  (7 raised; 16/21 skeptic votes refuted)

### [minor] paper/main.tex line 408 (Sec. V-D); paper/exploratory_tables.tex line 79 (Table VIII caption + its three data rows); root cause kdtraffic/exploratory.py:294-297 (calendar_overlap.weeks_of), same bug in scripts/17_design_figure.py:41-44
- **Paper says:** "Of the 18, 15 contain at least one week that another start date also tests; 55 unit-weeks are shared in total" and Table VIII: "55 unit-weeks in total", with Shared unit-weeks 23 / 23 / 9.
- **Problem:** The overlap accounting reconstructs each window's weeks naively from its name (`range(first, last+1)`) and so counts week 50, which the study excludes as low-coverage. Two windows are affected: start 11's `test_w48-51` actually contains weeks {48,49,51} (3 weeks, not 4) and start 24's `test_w49-51` contains {49,51} (2 weeks, not 3). Week 50 is therefore counted as a shared test week twice. The correct totals are 22 / 22 / 9 and 53 unit-weeks, not 23 / 23 / 9 and 55. The same naive span feeds Fig. 1(a), where the last test-window bars of starts 11 and 24 are drawn as unbroken 4- and 3-week blocks running through excluded week 50, and week 50 is also grey-shaded as "tested by >=2 starts" although no start date tests it. (The "15 of 18" count is unaffected.)
- **Evidence:** `python -c "from kdtraffic.data import evaluation_windows; print(evaluation_windows((15,15))[-1], evaluation_windows((28,28))[-1])"` -> `[48, 49, 51] [49, 51]` (kdtraffic/data.py:133-142 drops LOW_COVERAGE_WEEKS=(50,52)). Recomputing the shared-week tally from those true week lists gives `start 11: 22, start 24: 22, start 37: 9, total 53`, against `results/exploratory/REVISION_FULL3/calendar_overlap.csv` which stores `n_weeks=4` for test_w48-51, `n_weeks=3` for test_w49-51 and `weeks_shared... = 4 / 3`, summing to 55.
- **Suggested fix:** In `kdtraffic.exploratory.calendar_overlap`, filter `weeks_of()` through `LOW_COVERAGE_WEEKS` (and likewise in `scripts/17_design_figure.py`), re-run `scripts/12_exploratory.py` + `scripts/15_exploratory_tables.py` + `scripts/17_design_figure.py`, and change "55 unit-weeks" to "53" at main.tex:408. The design figure will then show the two final windows breaking at week 50, which is what the data does.

### [minor] paper/main.tex line 417 (Sec. V-F, Student Capacity)
- **Paper says:** "\kd{kdA4} is worse than the equally sized directly trained student at width 16 ($-0.054$ against \kd{direct}) and at width 96 ($-0.016$), under the energy score."
- **Problem:** The quoted numbers are not the equally-sized comparison the sentence describes. Every `<condition> - direct student` row in detection_advantage.csv is that condition minus the *registered width-48* `direct` student, not its own-width counterpart (scripts/12_exploratory.py:213-219 hard-codes `"direct"` as the baseline). The equally-sized gaps are -0.054 - (-0.024) = -0.030 at width 16 and -0.016 - (+0.002) = -0.018 at width 96 (neither is bootstrapped as a paired difference). The direction of the claim survives, but -0.054 overstates the width-16 KD penalty by ~1.8x, and "against \kd{direct}" sitting next to "the equally sized directly trained student" invites the reader to equate the two.
- **Evidence:** paper/exploratory_tables.tex rows 14-19: `direct_w16` $-0.024$, `direct_w96` $+0.002$, `kdA4_w16` $-0.054$, `kdA4_w96` $-0.016$ — all four are differences against the same `direct student` baseline, per the caption "A row naming one condition is that condition minus \kd{direct}" and scripts/12_exploratory.py:219 (`"comparison": f"{condition} - direct student"`).
- **Suggested fix:** Either reword to "...at width 16 ($-0.054$ against the registered-width \kd{direct}, against $-0.024$ for \kd{direct\_w16}) and at width 96 ($-0.016$ against $+0.002$)", or add paired `kdA4_w16 - direct_w16` / `kdA4_w96 - direct_w96` rows to the pair list in scripts/12_exploratory.py:228 and quote those.

### [minor] paper/main.tex line 344 (Sec. V preamble), affecting Fig. 5, Table V (tab:logitscores) and Table VI (tab:swaps)
- **Paper says:** "Intervals come from the same day$\times$service cluster bootstrap, resampling training seeds in the same way, so that they can be read against the confirmatory intervals." Fig. 5's caption: "bars are 95\% cluster bootstrap intervals"; Table V/VI captions: "95\% cluster bootstrap intervals".
- **Problem:** Every exploratory interval is built from 300 resamples, while Section III-F and Table III state 1,000 for the confirmatory family and the paper never says the exploratory count differs. Inviting the reader to read the two side by side without that fact overstates their comparability: with B=300 the smallest attainable one-sided p is 1/301 = 0.0033 (not 1/1001), and the 2.5%/97.5% percentiles rest on ~7 order statistics per tail. No number changes; the omission is one of reporting.
- **Evidence:** `python -c "import pandas as pd; print(pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv').n_boot.value_counts())"` -> `300  86`. swaps.csv likewise carries `n_boot=300` and `p_one_sided=0.003322` (=1/301) for every significant row, against `components.csv` `n_boot=1000`, `p_one_sided=0.000999` for the confirmatory rows. Defaults: scripts/12_exploratory.py:86 `--n-boot default=300`; scripts/16_run_revision.py:85 passes `"--n-boot", "300"`. The count appears nowhere in main.tex, paper/response-to-reviewers.md or docs/preregistration.md.
- **Suggested fix:** Add the count to the Section V preamble, e.g. "...the same day$\times$service cluster bootstrap at 300 resamples (against 1,000 for the confirmatory family), resampling training seeds in the same way...", or re-run scripts/12_exploratory.py with --n-boot 1000 so the two families match.

### [nit] paper/main.tex line 279 (Sec. IV-B), citing Fig. 4(c)
- **Paper says:** "...with closed-set macro-F1 for every student falling from about 0.90 to about 0.69 over 35 weeks (Fig.~\ref{fig:gap}(c))".
- **Problem:** The claim is true of the data but the cited panel does not show it. scripts/14_gap_figure.py draws only the four conditions in CLOSING+PERSISTING (kdA, directTS, ls, enddA); direct, kdB, kdA4 and kdB4 are absent from all three panels, so panel (c) displays four of the eight student conditions. The figure caption's lead-in "per condition and start date" and the panel titles "(a) Gap closes" / "(b) Gap persists" read as an exhaustive partition of the study's conditions, yet `direct` — the paper's central control, whose gap closes and crosses zero exactly like directTS — appears in neither panel.
- **Evidence:** scripts/14_gap_figure.py:29-30 `CLOSING = ("kdA", "directTS")`, `PERSISTING = ("ls", "enddA")`; the script's own stdout on regeneration: `72 gap points, 3 start dates, conditions ['directTS', 'enddA', 'kdA', 'ls']` (72 = 4 conditions x 18 units). Checking all eight from per_window.csv: macro-F1 max/min = direct 0.915/0.678, directTS 0.915/0.678, enddA 0.908/0.673, kdA 0.914/0.687, kdA4 0.892/0.674, kdB 0.913/0.681, kdB4 0.895/0.670, ls 0.907/0.672 — so "every student" holds, but four of them are not plotted.
- **Suggested fix:** Name the drawn conditions in the caption ("...for the four conditions shown") or change the text to "for every student (Table~\ref{tab:time} and Fig.~\ref{fig:gap}(c))"; alternatively add `direct` to CLOSING so the baseline the argument rests on is visible.

### [nit] paper/main.tex line 358 (Fig. 5 caption)
- **Paper says:** "Unknown-traffic detection advantage over the directly trained student, by scoring rule (exploratory; 18 test windows)..."
- **Problem:** The bottom block of the figure is not an advantage over the directly trained student: those three rows are paired student-versus-student differences (ls - kdA4, kdA4 - kdA, enddA - kdA), and `direct` is not one side of any of them. The caption's later sentence distinguishes the blocks only by feature dimensionality, not by what each block is differenced against. The y-axis labels do name both sides, so the figure itself is unambiguous; only the caption's opening clause over-generalises.
- **Evidence:** scripts/19_advantage_figure.py:43-45 `BLOCKS[2] = [("ls - kdA4", ...), ("kdA4 - kdA", ...), ("enddA - kdA", ...)]`, and detection_advantage.csv records these with `kind = "pair"` (scripts/12_exploratory.py:241) rather than `kind = "student"`. Rendered figure confirms the three bottom rows are labelled `ls - kdA4`, `kdA4 - kdA`, `enddA - kdA`.
- **Suggested fix:** Open the caption with "Unknown-traffic detection advantage by scoring rule (exploratory; 18 test windows). Each row is the difference its label names; the middle block is each condition minus the directly trained student, the bottom block is a paired student comparison."

### [nit] paper/main.tex line 399 (Sec. V-C), citing Table VII (tab:drift); generator scripts/15_exploratory_tables.py:86-92
- **Paper says:** "...with individual members between $-0.00261$ and $-0.00306$ (Table~\ref{tab:drift}). The two feature-space scores agree to within $0.00002$."
- **Problem:** The feature-space claim is correct but unsourced in the paper: drift_slopes.csv carries four scores (energy, msp, maha, knnfeat) and the table generator hard-codes only `("energy", "msp")`, so the Mahalanobis and feature k-NN slopes the sentence quotes are nowhere visible to a reader of the cited table.
- **Evidence:** `python -c "import pandas as pd; print(pd.read_csv('results/exploratory/REVISION_FULL3/drift_slopes.csv').pivot_table(index='model',columns='score',values='slope_per_week').round(6))"` -> ensemble maha -0.003541 vs member_mean -0.003528 (diff 0.000013); ensemble knnfeat -0.003490 vs member_mean -0.003499 (diff 0.000009) — both within 0.00002, confirming the claim. scripts/15_exploratory_tables.py:91 emits cells only `for s in ("energy", "msp")`.
- **Suggested fix:** Widen the drift table to all four score columns (change the tuple at scripts/15_exploratory_tables.py:91 and the header at :87 to iterate ALL_SCORES), so the sentence's evidence is on the page.

### [nit] paper/cpu_table.tex line 7 (Table IX column header) and line 3 (caption); generator scripts/11_cpu_table.py:38
- **Paper says:** Column header "Flows/s, all cores", with the caption stating the CPU is an "AMD Ryzen 7 5800H, 8 cores".
- **Problem:** The measurement is taken with `torch.set_num_threads(os.cpu_count())`, which on this machine is 16 logical processors, not 8 cores; the source column is named `flows_per_s_all_threads` and scripts/10_cpu_cost.py's own docstring calls it "all threads". Renaming it to "all cores" beside a caption that says the part has 8 cores implies an 8-way measurement that was not made.
- **Evidence:** results/cpu_cost/20260920-014955_S_train11-14/log.txt line 2: "CPU: AMD64 Family 25 Model 80 Stepping 0, AuthenticAMD; 16 logical cores"; scripts/10_cpu_cost.py:162 `threads_all = os.cpu_count()`, :170-172 `torch.set_num_threads(threads_all)` then `row["flows_per_s_all_threads"]`; scripts/10_cpu_cost.py:5 "throughput   flows per second at --batch-size, single thread and all threads".
- **Suggested fix:** Change the header at scripts/11_cpu_table.py:38 to "Flows/s, all threads" and regenerate, or note "(16 logical threads)" in the caption.

## internal-consistency  (10 raised; 0/0 skeptic votes refuted)

### [major] paper/main.tex line 417 (Section V-F, "Student Capacity")
- **Paper says:** "Distillation does not close the gap at any width. \kd{kdA4} is worse than the equally sized directly trained student at width 16 ($-0.054$ against \kd{direct}) and at width 96 ($-0.016$), under the energy score."
- **Problem:** The two numbers are NOT the equal-width comparison. Table VIII's rows are every condition minus the *registered width-48* \kd{direct} (its own caption: "A row naming one condition is that condition minus \kd{direct}"; the row "direct_w16 - direct student" = -0.024 proves the baseline is not same-width). The equal-width gaps are -0.030 at width 16 and -0.017 at width 96. The sentence overstates the width-16 gap by 1.8x and makes the gap look strongly width-dependent (-0.054 -> -0.026 -> -0.016) when the real sequence is nearly flat (-0.030 -> -0.026 -> -0.017). The conclusion (kdA4 worse at every width) survives; the numbers supporting it do not.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c on results/exploratory/REVISION_FULL3/detection_advantage.csv (score=='energy'): kdA4_w16 - direct student = -0.0538; direct_w16 - direct student = -0.0240 => equal-width gap = -0.0297. kdA4_w96 - direct student = -0.0157; direct_w96 - direct student = +0.0016 => equal-width gap = -0.0173. Registered width: kdA4 - direct = -0.0261.
- **Suggested fix:** Rewrite as: "\kd{kdA4} is worse than the equally sized directly trained student at width 16 ($-0.030$), at the registered width 48 ($-0.026$) and at width 96 ($-0.017$), under the energy score" - or keep -0.054/-0.016 but say explicitly "against the registered width-48 \kd{direct}", which is what Table VIII reports.

### [major] paper/main.tex line 220 (Table III / tab:hypotheses, H2[energy] row) vs line 254 (Section IV-B) and Table II
- **Paper says:** Table III H2[energy] estimate cell: "AUROC vs \kd{ls} $+0.021$, vs \kd{directTS} $0.000$; NLL vs \kd{ls} $-0.040$, vs \kd{directTS} $+0.008$"
- **Problem:** The two halves of the same cell use opposite operand orders. The AUROC entries are kdA minus control (+0.021 = kdA better). The NLL entries are control minus kdA (components.csv names them nll_ls_minus_kdA and nll_directTS_minus_kdA), so -0.040 means kdA is WORSE calibrated than \kd{ls} and +0.008 means kdA is BETTER than \kd{directTS}. A reader applying the AUROC convention to the NLL entries gets exactly the opposite of both, and concludes distillation is better calibrated than label smoothing - which contradicts line 254 ("is worse calibrated (post-hoc NLL 0.601 vs 0.561)"), Contribution 2 ("against label smoothing it trades detection for calibration") and Table II (kdA 0.601, ls 0.561). The "three conventions" paragraph at line 233 does not mention this orientation, and \kd{directTS}'s post-hoc NLL is printed as "---" in Table II, so the +0.008 cannot be reconstructed from the table either. (H4b2's row is saved only by its parenthetical "(detection improves)".)
- **Evidence:** results/analysis/CONFIRMATORY/components.csv: H2[energy] auroc_kdA_minus_ls = +0.020977, auroc_kdA_minus_directTS = -0.000032, nll_ls_minus_kdA = -0.039922, nll_directTS_minus_kdA = +0.008171. Table II (verified against per_window.csv, flows=='all', unknown=='all'): nll_ts kdA = 0.60056, ls = 0.56063, direct = 0.60873 -> kdA minus ls = +0.040 (kdA worse), kdA minus directTS = -0.008 (kdA better).
- **Suggested fix:** Either write the NLL entries in the same order as the AUROC entries ("NLL vs \kd{ls} $+0.040$, vs \kd{directTS} $-0.008$"), or add one sentence to the caption/conventions paragraph: "every entry is signed so that positive favours the hypothesis; for NLL, where lower is better, that is control minus KD."

### [major] paper/main.tex line 424 (Section V-G) vs line 383 (Section V-B)
- **Paper says:** Line 424: "Its closed-set accuracy on the validation week is $0.945$ against the directly trained student's $0.946$, and its macro-F1 $0.918$ against $0.919$."  Line 383: "\kd{hardA} ... matching the directly trained student's closed-set accuracy on the validation week ($0.913$ against $0.913$)"
- **Problem:** Three mutually incompatible statements about the same model on the same week. (a) The direct student's validation-week closed-set accuracy is 0.941 (all flows) or 0.950 (>=5 packets); it is never 0.946. 0.946 is the average of the two flow subsets - a quantity with no meaning - and 0.919 is the same average of the macro-F1s. (b) Line 383's 0.913 is not the accuracy at all: it is the macro-F1 (accuracy is 0.941 for both hardA and direct). So Section V-B tells the reader the direct student's validation accuracy is 0.913 and Section V-G tells him it is 0.946, and neither is the value in the metrics file (0.941).
- **Evidence:** results/track_a/20260919-113607_S_train11-14/metrics.csv, split=='val', unknown=='all', mean over 3 seeds: direct flows=='all' accuracy 0.9410 / macro_f1 0.9131; flows=='ge5' accuracy 0.9501 / macro_f1 0.9244; mean of the two subsets = 0.9456 (-> 0.946) and 0.9188 (-> 0.919). results/track_a_exploratory/20260923-051258_S_train11-14/metrics.csv: kdF flows=='all' accuracy 0.9407 / macro_f1 0.9121; flows=='ge5' 0.9497 / 0.9229; mean of subsets 0.9452 (-> 0.945). results/track_a_exploratory/20260922-130934_S_train11-14/metrics.csv: hardA flows=='all' accuracy 0.9406, macro_f1 0.9125 (-> 0.913).
- **Suggested fix:** Use one flow subset (all flows, matching everything else in the paper) and label it correctly. Line 424: "Its closed-set accuracy on the validation week is $0.941$ against the directly trained student's $0.941$, and its macro-F1 $0.912$ against $0.913$." Line 383: change "closed-set accuracy" to "closed-set macro-F1" (0.913 against 0.913), or quote the accuracy, 0.941 against 0.941.

### [major] paper/main.tex line 56 (Contribution 3), line 279 (Section IV-C), line 443 (Discussion), line 29 (abstract)
- **Paper says:** Contribution 3: "after about three months the student is the better detector." Discussion: "from about the third month the student was the better detector." Section IV-C: "from roughly the third month onwards the student is the better unknown detector."
- **Problem:** Stated as a general result, but it holds only for start date 11 (and the single 11-week window of start date 37). At start date 24 the teacher leads in all six windows, including all four beyond three months (+0.0085, +0.0040, +0.0044, +0.0057 energy AUROC). Pooled over all 18 windows the teacher is still ahead by +0.001 (CI -0.003 to +0.005), i.e. the crossing is not established on the full design. The paper's own Fig. 5(a) shows this - the dashed (start wk 24) lines stay above zero for their whole span - and Section IV-C prefaces the claim with "Table V follows start date 11", but the qualifier is dropped in the contributions list, the discussion and the abstract. Section V-E is careful to show that the *slope* reversal holds in all three series; it says nothing about the crossing, which does not.
- **Evidence:** results/analysis/CONFIRMATORY/units.csv (analysis=='primary'), gap = auroc_energy_teacherA - auroc_energy_kdA. start 24: weeks 3.5/7.5/11.5/15.5/19.5/23.0 -> +0.0017, +0.0033, +0.0085, +0.0040, +0.0044, +0.0057 (teacher ahead in every window). start 11: crosses at 11.5 weeks and stays negative. start 37: -0.0088 at 11.0 weeks. components.csv info row mean_gap_teacherA_minus_kdA_energy = +0.001091 (CI -0.002682, +0.005112). Fig. paper/figures/fig_gap_split.png panel (a): the start-wk-24 dashed curve never reaches zero.
- **Suggested fix:** Scope the claim wherever it appears: e.g. Contribution 3 "...and in the longest-running replicate the student is the better detector from about the third month"; Discussion likewise, adding one clause that the crossing is seen at start dates 11 and 37 but not at 24, where the teacher keeps a small lead for the 23 weeks it is observed.

### [minor] paper/main.tex line 385 (Section V-B, "Across model families the effect is larger")
- **Paper says:** "Teacher C is a transformer encoder over the packet sequence (3.5M parameters), trained on start date 11 only and within 0.4 macro-F1 points of Teacher A."
- **Problem:** 0.4 points is the *accuracy* difference, not the macro-F1 difference. Teacher C is 0.62 macro-F1 points below Teacher A on the validation week. This is the same accuracy/macro-F1 slip as at line 383/424, and it matters here because the sentence is the matched-teacher justification for the largest shift reported in the paper (+0.043).
- **Evidence:** results/track_a_exploratory/20260923-032036_S_train11-14/metrics.csv, split=='val', flows=='all', unknown=='all': teacherC accuracy 0.969945 / macro_f1 0.957675; teacherA accuracy 0.973680 / macro_f1 0.963854. Accuracy gap 0.00374 (0.37 pts); macro-F1 gap 0.00618 (0.62 pts). On flows=='ge5' the macro-F1 gap is 0.56 pts.
- **Suggested fix:** "...and within 0.7 macro-F1 points of Teacher A" (or "within 0.4 accuracy points"). Note this is still inside the 2-point teacher-matching tolerance of Section III-B, so nothing else changes.

### [minor] paper/main.tex line 183 (Section IV-A), against Table II on the same page
- **Paper says:** "The teachers are 5--6 macro-F1 points more accurate than any student"
- **Problem:** Contradicted by the table the sentence is introducing. The smallest teacher-student gap is 4.8 points (Teacher B vs \kd{kdA}) and the largest is 7.1 points (Teacher A vs \kd{kdB4}); a reader subtracting Table II's own printed values gets 0.855-0.806 = 4.9 and 0.860-0.789 = 7.1, neither inside "5--6".
- **Evidence:** results/analysis/CONFIRMATORY/per_window.csv, flows=='all' & unknown=='all', mean macro_f1_mean over the 18 windows: teacherA 0.86010, teacherB 0.85461; kdA 0.80650, kdB 0.80603, direct/directTS 0.80569, enddA 0.80052, ls 0.79656, kdA4 0.79026, kdB4 0.78930. min gap = 0.85461-0.80650 = 0.0481; max gap = 0.86010-0.78930 = 0.0708.
- **Suggested fix:** "The teachers are 5 to 7 macro-F1 points more accurate than any student".

### [minor] paper/main.tex line 351 (Section V-A) and line 29 (abstract), vs line 362 (Section V-A)
- **Paper says:** Line 351: "Under those scores Teacher A and the directly trained student differ by $0.001$ energy AUROC." Abstract: "...the teacher detects unknown traffic $0.073$ AUROC better than the direct student, where the energy score sees $0.001$." Line 362: "...against $0.000$ under the energy score ($-0.004$ to $0.004$)."
- **Problem:** The same quantity (Teacher A minus \kd{direct}, energy AUROC) is printed as 0.001 in the abstract and in the opening of Section V-A and as 0.000 eleven lines later. The source value is +0.0004, i.e. 0.000. The 0.001 appears to have been carried over from Section IV-A, where 0.001 is correct but is Teacher A minus \kd{kdA}, a different pair. Small, but it is the number the whole scoring-rule argument pivots on, and the paper states it two ways.
- **Evidence:** results/exploratory/REVISION_FULL3/detection_advantage.csv: score=='energy', comparison=='teacherA - direct student' -> estimate 0.000387, CI (-0.003606, 0.004065). results/analysis/CONFIRMATORY/components.csv: info row mean_gap_teacherA_minus_kdA_energy = 0.001091 (CI -0.002682, 0.005112) - the Section IV-A number, against kdA not direct.
- **Suggested fix:** Change line 351 to "differ by $0.000$ energy AUROC" and the abstract to "where the energy score sees $0.000$"; or say "0.001" in both places only if the comparison is restated as against \kd{kdA}.

### [minor] paper/figures/fig_gap_split.png, panel (c) title; caption at paper/main.tex line 302
- **Paper says:** Panel (c) is titled "Student accuracy"; its y-axis is labelled "macro-F1"; the caption says "(c) Student macro-F1 over the same axis"; the prose (line 279) says "closed-set macro-F1 for every student falling from about 0.90 to about 0.69".
- **Problem:** The panel title contradicts its own axis label, the caption and the prose. In a paper whose central argument is that accuracy is not the only thing worth measuring - and which reports accuracy and macro-F1 as genuinely different numbers (0.941 vs 0.913 on validation) - using "accuracy" as a loose synonym for macro-F1 in a figure is the one place it should not appear.
- **Evidence:** Read of paper/figures/fig_gap_split.png: panel (c) header text "(c) Student accuracy", y-axis text "macro-F1". paper/main.tex line 302 caption: "(c) Student macro-F1 over the same axis." Values plotted run 0.90 -> 0.68, matching per_window.csv macro_f1_mean for start 11 (direct 0.901 -> 0.678), not accuracy.
- **Suggested fix:** Retitle panel (c) "Student macro-F1" in the figure-generating script and regenerate.

### [minor] paper/main.tex line 29 (abstract) vs line 254 (Section IV-B), line 55 (Contribution 2) and line 438 (Discussion)
- **Paper says:** Abstract: "Under the logit-based scores nothing else transfers: distillation beats neither a temperature-scaled direct student nor label smoothing."
- **Problem:** "Beats neither" is exact for \kd{directTS} (a genuine tie) but misdescribes label smoothing, which distillation beats clearly on the paper's primary outcome: +0.021 energy AUROC and +0.006 MSP AUROC, both intervals clear of zero. The body says so three times ("the picture is mixed"; "it trades detection for calibration"). A reader of the abstract alone takes away that distillation has no advantage over label smoothing under the pre-registered scores, which the body contradicts.
- **Evidence:** results/analysis/CONFIRMATORY/components.csv: H2[energy] auroc_kdA_minus_ls = +0.020977 (CI 0.014003, 0.027191), p_one_sided 0.000999; H2[msp] auroc_kdA_minus_ls = +0.006214 (0.003584, 0.008742). By contrast auroc_kdA_minus_directTS = -0.000032 (energy) and -0.002253 (MSP).
- **Suggested fix:** "...distillation does not beat a temperature-scaled direct student, and against label smoothing it trades detection for calibration" - the wording already used in Contribution 2.

### [nit] paper/main.tex lines 362 and 374 (Section V-A), vs Table III (line 227) and results/exploratory/REVISION_FULL3/detection_advantage.csv
- **Paper says:** Line 374: "the worst detector in the grid under the energy score ($-0.092$ against \kd{kdA}, CI $-0.100$ to $-0.085$)". Line 362: "$0.034$ better under the feature $k$-NN score ($0.028$ to $0.040$)".
- **Problem:** Two printed interval endpoints match neither the source CSV nor Table III. The enddA-kdA energy interval is (-0.0995, -0.0847) in the exploratory run and (-0.0992, -0.0844) in the confirmatory one; -0.100 is not either, and Table III prints (-0.099, -0.084) for the identical quantity, so the paper gives the same CI two different ways. The feature k-NN upper bound is 0.0395, i.e. 0.039, not 0.040. All other endpoints in this subsection check out exactly (0.073 CI 0.064-0.082; kdA4 0.076 CI 0.069-0.083; ls-kdA4 0.011 CI 0.008-0.015; etc.), so these two look like stray edits rather than a regeneration.
- **Evidence:** results/exploratory/REVISION_FULL3/detection_advantage.csv: (score=='energy', comparison=='enddA - kdA') estimate -0.092009, ci (-0.099485, -0.084715); (score=='knnfeat', comparison=='teacherA - direct student') estimate 0.033806, ci (0.028033, 0.039454). results/analysis/CONFIRMATORY/components.csv H5[energy] auroc_enddA_minus_kdA: -0.092009, ci (-0.099237, -0.084368) - the values Table III prints as (-0.099, -0.084).
- **Suggested fix:** Line 374: "CI $-0.099$ to $-0.085$" (and consider making Table III's H5[energy] row read the same, since the two runs agree to three decimals). Line 362: "($0.028$ to $0.039$)".

## numbers-confirmatory  (11 raised; 0/0 skeptic votes refuted)

### [major] paper/main.tex line 235 (Section IV, and Table III's "reversed" outcome for H3[energy], line 222)
- **Paper says:** "The two sensitivity analyses (exact duplicates of training flows removed, 1.8\% of test flows; flows with at least five packets only, 94.0\% of test flows) reach the same decision on every hypothesis, with every $p$-value within 0.1 of the primary analysis."
- **Problem:** Literally true for the supported/not-supported decision, but it conceals that the paper's flagship *reversal* of H3 under the energy score is not robust. Table III labels H3[energy] "reversed" because the slope's 95% CI excludes zero. Under the >=5-packet sensitivity analysis the same slope's CI spans zero, so the reversal (as opposed to "not supported") loses its evidential basis on one of the two co-primary scores. A reader is told the sensitivity analyses agree on everything and will infer the reversal is robust; it is not. (The duplicate-removal analysis does preserve it, as does MSP under both.)
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; c=pd.read_csv('results/analysis/CONFIRMATORY/components.csv'); print(c[c.hypothesis=='H3[energy]'][['estimate','ci_low','ci_high','p_one_sided','analysis']])"  ->  primary: est -0.000381, CI (-0.000685, -0.000077), p 0.993007; drop_duplicates: -0.000330, CI (-0.000623, -0.000047); ge5_packets: -0.000222, CI (-0.000534, +0.000078), p 0.927073  <-- CI spans zero
- **Suggested fix:** Add one clause, e.g. "...reach the same decision on every hypothesis, with every $p$-value within 0.1 of the primary analysis; the only material change is that H3[energy]'s reversal weakens when flows of fewer than five packets are dropped ($-0.0002$ per week, CI $-0.0005$ to $+0.0001$), while H3[MSP] and the duplicate-removal analysis keep it."

### [minor] paper/main.tex line 29 (abstract) and line 351 (Section V-A)
- **Paper says:** abstract: "...the teacher detects unknown traffic $0.073$ AUROC better than the direct student, where the energy score sees $0.001$"; line 351: "Under those scores Teacher A and the directly trained student differ by $0.001$ energy AUROC."
- **Problem:** Teacher A minus the *directly trained* student under the energy score is 0.0004, which rounds to 0.000, not 0.001. The value 0.001 is Teacher A minus \kd{kdA} (the T=1 distilled student), a different comparison. The paper itself states the correct figure eleven lines later at line 362 ("against $0.000$ under the energy score ($-0.004$ to $0.004$)"), so the manuscript contradicts itself on the same quantity in three places.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[(d.score=='energy')&(d.comparison=='teacherA - direct student')])"  ->  estimate 0.000387, CI (-0.003606, 0.004065).  Cross-check on the confirmatory table: per_window.csv, unknown=='all', flows=='all', mean auroc_energy teacherA 0.835569 vs direct 0.835188 -> 0.000381.  (Teacher A minus kdA is 0.001081, i.e. components.csv info/mean_gap_teacherA_minus_kdA_energy = 0.0010906.)
- **Suggested fix:** Change both occurrences of "$0.001$" to "$0.000$" so they match line 362, or say explicitly "differ by $0.001$ from \kd{kdA} and by $0.000$ from \kd{direct}".

### [minor] paper/main.tex line 374 (Section V-A), against Table III (line 227)
- **Paper says:** "the ensemble-distribution-distillation student, the worst detector in the grid under the energy score ($-0.092$ against \kd{kdA}, CI $-0.100$ to $-0.085$)"
- **Problem:** The lower CI bound is mis-rounded in the third decimal. The exploratory run gives $-0.099485$, which rounds to $-0.099$, not $-0.100$. As printed it also disagrees with Table III, which gives the same hypothesis's interval as $(-0.099, -0.084)$ from the confirmatory bootstrap, making the two look like different numbers when they are the same quantity measured twice.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[(d.score=='energy')&(d.comparison=='enddA - kdA')])"  ->  estimate -0.092009, ci_low -0.099485, ci_high -0.084715.  Confirmatory: components.csv H5[energy]/auroc_enddA_minus_kdA/primary -> -0.092009, (-0.099237, -0.084368).
- **Suggested fix:** Write "CI $-0.099$ to $-0.085$", or quote the confirmatory interval $(-0.099, -0.084)$ and note that the exploratory re-run reproduces it.

### [minor] paper/main.tex line 233 (Section IV, reading conventions for Table III)
- **Paper says:** "With 1,000 resamples the smallest attainable one-sided $p$ is $1/1001 = 0.000999$, so 0.009 is the smallest Holm-corrected value a family of ten can produce"
- **Problem:** Incorrect. Holm multiplies the *smallest* p-value in the family by m = 10, so the smallest attainable Holm-adjusted value is 10 x 0.000999 = 0.00999, i.e. 0.010. H1's 0.009 arises only because H1 ranked second: H4b1's paired t-test p (0.000473) is smaller and took the x10 multiplier, leaving H1 with x9. The stated floor is therefore both wrong in general and wrong as an explanation of the 0.009 in the table.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; h=pd.read_csv('results/analysis/CONFIRMATORY/hypotheses.csv'); print(h)"  ->  H4b1 p_iut 0.00047310, p_holm 0.00473104 (= 10 x p); H1 p_iut 0.00099900, p_holm 0.00899101 (= 9 x p). And 10 x 1/1001 = 0.00999.
- **Suggested fix:** "...so a bootstrap-based hypothesis can reach at best $10\times0.000999 = 0.010$ after Holm correction, and H1's 0.009 reflects its second rank behind H4b1's $t$-test."

### [minor] paper/main.tex line 108 (Section III, Time structure)
- **Paper says:** "This yields 9, 6 and 3 test windows, 18 in total, each a stratified sample of 100k known and 100k test-unknown flows"
- **Problem:** Not true of all 18 windows. The final window of start date 37 (test_w51-51, a single week) has only 43,579 test-unknown flows against the stated 100k. As written the reader computes 18 x 200k = 3.6M test flows, which contradicts the paper's own "3.5 million test flows" at line 240. The true total is 3,543,579.
- **Evidence:** results/track_a/20260919-143354_S_train37-40/log.txt: "test W-2022-51-51: kept 143,579 of 143,579 flows (43,579 unknown)"; confirmed in metrics.csv (n_known 100000, n_unknown 43579). Summing all 18 windows: C:/venvs/kd-traffic/Scripts/python.exe -> total 3,543,579 flows (which is what makes the 94.0% >=5-packet figure and the "3.5 million" correct).
- **Suggested fix:** "...each a stratified sample of 100k known and up to 100k test-unknown flows (the final one-week window of start date 37 has 43.6k unknowns), 3.54M test flows in total..."

### [minor] paper/main.tex line 235 (Section IV)
- **Paper says:** "Two sensitivity analyses repeat everything with exact duplicates of training flows removed and with flows of fewer than five packets removed" (line 175) / "...reach the same decision on every hypothesis" (line 235)
- **Problem:** The sensitivity analyses cover 7 of the 10 hypotheses, not all ten. H4a, H4b1 and H4b2 are computed only under the primary analysis, because the shortcut experiment runs on the validation week and neither sensitivity rule applies to it. "Repeat everything" and "every hypothesis" overstate the coverage.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; c=pd.read_csv('results/analysis/CONFIRMATORY/components.csv'); print(c[c.hypothesis.str.startswith('H4')][['hypothesis','analysis']])"  ->  H4a, H4b1, H4b2 appear with analysis=='primary' only; drop_duplicates and ge5_packets contain H1, H2[energy], H2[msp], H3[energy], H3[msp], H5[energy], H5[msp] only.
- **Suggested fix:** "...reach the same decision on each of the seven test-window hypotheses (H4 runs on the validation week and is unaffected by either rule)..."

### [minor] paper/main.tex line 283 (caption of Table V, tab:time)
- **Paper says:** "Every second test window is shown."
- **Problem:** The table shows 6 of the 9 windows of start date 11: 3.5, 7.5, 11.5, 19.5, 27.5, 35.3. The first three are consecutive, then every second one. "Every second test window" would be 5 rows (3.5, 11.5, 19.5, 27.5, 35.3). The caption misdescribes the selection.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/analysis/CONFIRMATORY/per_window.csv'); print(sorted(d[(d.start==11)&(d.condition=='kdA')&(d.unknown=='all')&(d.flows=='all')].weeks_since.unique()))"  ->  [3.5, 7.5, 11.5, 15.5, 19.5, 23.5, 27.5, 31.5, 35.333]; the table prints 3.5, 7.5, 11.5, 19.5, 27.5, 35.3.
- **Suggested fix:** "A subset of the nine test windows is shown." (All six printed values themselves check out exactly against per_window.csv.)

### [minor] paper/main.tex line 417 (Section V-F, Student Capacity)
- **Paper says:** "\kd{kdA4} is worse than the equally sized directly trained student at width 16 ($-0.054$ against \kd{direct}) and at width 96 ($-0.016$), under the energy score."
- **Problem:** The sentence claims a comparison against the *equally sized* direct student, but both quoted numbers are measured against the registered width-48 \kd{direct}. Against the equally sized controls the gaps are $-0.030$ (width 16) and $-0.017$ (width 96). The qualitative conclusion survives, but as written the numbers do not measure what the sentence says they measure.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); e=d[d.score=='energy'].set_index('comparison').estimate; print(e['kdA4_w16 - direct student'], e['direct_w16 - direct student'], e['kdA4_w96 - direct student'], e['direct_w96 - direct student'])"  ->  -0.053771, -0.024023, -0.015744, +0.001584.  So kdA4_w16 - direct_w16 = -0.029748 and kdA4_w96 - direct_w96 = -0.017328.
- **Suggested fix:** Either state the like-for-like gaps ($-0.030$ at width 16, $-0.017$ at width 96) or reword to "...is worse than the registered-width \kd{direct} student at width 16 ($-0.054$) and at width 96 ($-0.016$)".

### [nit] paper/main.tex line 362 (Section V-A)
- **Paper says:** "$0.034$ better under the feature $k$-NN score ($0.028$ to $0.040$)"
- **Problem:** Upper CI bound mis-rounded in the third decimal: 0.039454 rounds to 0.039, not 0.040.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[(d.score=='knnfeat')&(d.comparison=='teacherA - direct student')])"  ->  estimate 0.033806, ci_low 0.028033, ci_high 0.039454.
- **Suggested fix:** "($0.028$ to $0.039$)"

### [nit] paper/main.tex line 401 (Section V-C, Does the Ensemble Age Faster than Its Members?)
- **Paper says:** "The ensemble ages at $-0.00306$ per week against its members' $-0.00282$, an excess of $0.00024$"
- **Problem:** The excess is 0.000233, which rounds to 0.00023 at the precision used for the two operands. The argument ("aggregation could account for most of the MSP narrowing of 0.00030") is unaffected.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/drift_slopes.csv'); m=d[d.score=='msp'].set_index('model').slope_per_week; print(m['ensemble'], m['member_mean'], m['ensemble']-m['member_mean'])"  ->  -0.0030573, -0.0028248, -0.00023251.
- **Suggested fix:** "an excess of $0.00023$"

### [nit] paper/main.tex line 279 (Section IV-B)
- **Paper says:** "Table~\ref{tab:time} follows start date 11. ... Under MSP the teacher holds a lead of about $0.026$ for the first thirty weeks and $0.011$ in the last window"
- **Problem:** The two figures come from different bases. 0.026 is the mean MSP gap pooled over all 18 windows with weeks_since < 30; 0.011 is start date 11's last window only. In the start-11 series the paragraph says it is following, the pre-thirty-week mean is 0.023, so a reader checking the sentence against its stated basis gets a different number.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/analysis/CONFIRMATORY/per_window.csv'); h=d[(d.unknown=='all')&(d.flows=='all')&d.condition.isin(['teacherA','kdA'])]; p=h.pivot_table(index=['start','weeks_since'],columns='condition',values='auroc_msp_mean'); p['gap']=p.teacherA-p.kdA; print(p[p.index.get_level_values(1)<30].gap.mean(), p.loc[11].gap.iloc[:8].mean(), p.loc[11].gap.iloc[-1])"  ->  0.026091 (all starts, <30 weeks), 0.023063 (start 11, <30 weeks), 0.010666 (start 11, last window).
- **Suggested fix:** Say which series each number comes from, e.g. "...a lead of about $0.026$ across all units in the first thirty weeks, and $0.011$ in start date 11's final window".

## numbers-exploratory  (10 raised; 8/30 skeptic votes refuted)

### [major] paper/exploratory_tables.tex line 6 (caption of Table \ref{tab:logitscores}); generated by scripts/15_exploratory_tables.py lines 184-188
- **Paper says:** "Intervals are 95\% cluster bootstrap intervals over the 18 test windows, or over the nine of start date 11 for the conditions trained there alone (the width sweep, \kd{kdC} and \kd{kdF})."
- **Problem:** The enumeration is incomplete. The Mahalanobis and feature k-NN cells for \kd{kdM0}, \kd{kdM1} and \kd{hardA} -- and the feature-space cells of the \kd{hardA} - \kd{kdA4} paired row -- are also pooled over only the nine windows of start date 11, because feature scores for those three conditions were computed at start 11 only (results/feature_scores/20260923-090027_S_train11-14). Those conditions were NOT "trained there alone": their energy and MSP cells in the same rows use all 18 windows. So three rows of the table silently mix an 18-unit logit estimate with a 9-unit feature-space estimate, and the caption tells the reader they are all 18. This is genuinely misleading, not just imprecise: as printed, \kd{kdM0}'s Mahalanobis $+0.078$ reads as beating \kd{kdA4}'s $+0.076$ and the teacher's $+0.073$ (Sec. V-A). On the matched nine windows the teacher has $+0.0835$ and \kd{kdA4} has $+0.0828$, so \kd{kdM0} ($+0.0776$) is in fact below both. The unit-set mismatch inverts the ranking.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[d.comparison.str.contains('kdM0|kdM1|hardA')][['score','comparison','estimate','units_used']].to_string())"  ->  maha/knnfeat rows for kdM0, kdM1, hardA and for the pair 'hardA - kdA4' all have units_used=9, while their energy/msp rows have units_used=18.

Independent confirmation from the raw feature scores:
C:/venvs/kd-traffic/Scripts/python.exe -c "...glob results/feature_scores/*_S_train*/metrics.csv, test splits, flows=='all', unknown=='all', per-condition mean minus direct..."  ->
  kdM0   n= 9 mean=+0.0776  starts=[11]
  kdM1   n= 9 mean=+0.0705  starts=[11]
  hardA  n= 9 mean=+0.0182  starts=[11]
  kdF    n= 9 mean=+0.0137  starts=[11]
  kdA4   n=18 mean=+0.0763  starts=[11, 24, 37]
  ls     n=18 mean=+0.0876  starts=[11, 24, 37]
and, restricted to start 11 for a matched comparison:
  teacherA start11=+0.0835, kdA4 start11=+0.0828, kdM0 start11=+0.0776 (Mahalanobis)
  teacherA start11=+0.0373, kdA4 start11=+0.0284, kdM0 start11=+0.0270 (feature k-NN)
Also: results/feature_scores/20260923-090027_S_train11-14/metrics.csv contains only start-11 windows and only the hardA/kdM0/kdM1 models.
- **Suggested fix:** Edit the caption string in scripts/15_exploratory_tables.py (lines 186-188) and regenerate. Rather than hand-listing the 9-unit conditions, derive it from the data, e.g. append a per-cell marker (a dagger) wherever units_used < 18, or reword to: "Intervals are 95% cluster bootstrap intervals over the 18 test windows, except where marked: the width sweep, kdC and kdF were trained on start date 11 only, and the feature-space scores for kdM0, kdM1 and hardA were computed on start date 11 only, so those cells pool over nine windows." Since the ranking inversion is the real hazard, also consider adding the start-11-matched teacher and kdA4 Mahalanobis figures (+0.084 and +0.083) so the kdM0 row can be read correctly.

### [minor] paper/main.tex line 385 (Section V-B, "Across model families the effect is larger")
- **Paper says:** "Teacher C is a transformer encoder over the packet sequence (3.5M parameters), trained on start date 11 only and within 0.4 macro-F1 points of Teacher A."
- **Problem:** The validation macro-F1 gap between Teacher A and Teacher C is 0.62 points, not within 0.4. The 0.37-point figure is the *accuracy* gap, so this looks like an accuracy number labelled as macro-F1. (The parameter count, 3,495,270, is correct.) The substance survives -- the pre-registered teacher-match tolerance is 2 macro-F1 points on validation (docs/preregistration.md line 40) and 0.62 is well inside it -- but the stated number is wrong, and it understates the mismatch in the arm that produces the paper's largest effect. Note also that over the nine start-11 test windows the gap is 1.36 macro-F1 points.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; m=pd.read_csv('results/track_a_exploratory/20260923-032036_S_train11-14/metrics.csv'); v=m[(m.split=='val')&(m.flows=='all')&(m.unknown=='all')]; print(v[['model','accuracy','macro_f1']].to_string())"
  -> teacherA  accuracy 0.973680  macro_f1 0.963854
     teacherC  accuracy 0.969945  macro_f1 0.957675
  macro-F1 gap = 0.963854 - 0.957675 = 0.006179 = 0.62 points
  accuracy gap = 0.973680 - 0.969945 = 0.003735 = 0.37 points
Over the start-11 test windows: teacherA macro_f1 0.830844, teacherC 0.817248 -> 1.36 points.
Parameter count from results/track_a_exploratory/20260923-032036_S_train11-14/log.txt line 20: "teacherC_transformer (transformer_teacher, 3,495,270 parameters".
- **Suggested fix:** Replace with "within 0.6 macro-F1 points of Teacher A on the validation week (1.4 points averaged over the nine test windows), inside the 2-point tolerance the registration sets for the teacher-swap comparison".

### [minor] paper/main.tex line 29 (abstract) and line 351 (Section V-A, first paragraph)
- **Paper says:** Abstract: "the teacher detects unknown traffic $0.073$ AUROC better than the direct student, where the energy score sees $0.001$". Line 351: "Under those scores Teacher A and the directly trained student differ by 0.001 energy AUROC."
- **Problem:** Both sentences attribute 0.001 to the Teacher A minus *direct student* energy-AUROC gap. That quantity is 0.00039, which line 362 of the same section correctly reports as 0.000. The value 0.001 is the Teacher A minus \kd{kdA} gap from the confirmatory analysis (0.001091, Table \ref{tab:headline} and line 183) -- a different comparison. The abstract sentence is internally inconsistent, pairing a teacher-vs-direct number (0.073) with a teacher-vs-kdA number (0.001) as if they were the same contrast, and it contradicts line 362 three paragraphs later.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[(d.score=='energy')&(d.comparison=='teacherA - direct student')].to_string())"
  -> estimate 0.00038728, ci (-0.00360573, 0.00406450), units_used 18
C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; c=pd.read_csv('results/analysis/CONFIRMATORY/components.csv'); print(c[c.component=='mean_gap_teacherA_minus_kdA_energy'].head(1).to_string())"
  -> estimate 0.001091, ci (-0.002682, 0.005112)  [this is teacherA - kdA, not teacherA - direct]
Main.tex line 362 states "against $0.000$ under the energy score ($-0.004$ to $0.004$)" for the same teacher-vs-direct pair.
- **Suggested fix:** Change both occurrences to 0.000 so they match line 362, e.g. abstract: "...where the energy score sees $0.000$"; line 351: "Under those scores Teacher A and the directly trained student differ by 0.000 energy AUROC (CI $-0.004$ to $0.004$)."

### [minor] paper/main.tex line 362 (Section V-A)
- **Paper says:** "...and $0.034$ better under the feature $k$-NN score ($0.028$ to $0.040$)"
- **Problem:** The upper bound of the 95% cluster bootstrap interval is 0.03945, which rounds to 0.039 at the three decimal places the paper uses everywhere else. 0.040 is a rounding error in the last digit.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); r=d[(d.score=='knnfeat')&(d.comparison=='teacherA - direct student')].iloc[0]; print(r.estimate, r.ci_low, r.ci_high)"
  -> 0.0338059494100952 0.028033104073840215 0.039453903617234456
- **Suggested fix:** Change "($0.028$ to $0.040$)" to "($0.028$ to $0.039$)".

### [minor] paper/main.tex line 374 (Section V-A, final paragraph)
- **Paper says:** "the ensemble-distribution-distillation student, the worst detector in the grid under the energy score ($-0.092$ against \kd{kdA}, CI $-0.100$ to $-0.085$)"
- **Problem:** The lower CI bound is -0.0995 in the exploratory run and -0.0992 in the confirmatory run; both round to -0.099, not -0.100. Same last-digit rounding error as the previous finding.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); r=d[(d.score=='energy')&(d.comparison=='enddA - kdA')].iloc[0]; print(r.estimate, r.ci_low, r.ci_high)"
  -> -0.09200906279837302 -0.09948531010322798 -0.08471479163213776
and results/analysis/CONFIRMATORY/components.csv, H5[energy] auroc_enddA_minus_kdA: estimate -0.092009, ci_low -0.099237, ci_high -0.084368.
- **Suggested fix:** Change "CI $-0.100$ to $-0.085$" to "CI $-0.099$ to $-0.085$".

### [minor] paper/main.tex line 401 (Section V-D, "Under MSP it is a different matter")
- **Paper says:** "The ensemble ages at $-0.00306$ per week against its members' $-0.00282$, an excess of $0.00024$"
- **Problem:** Double rounding: the excess is computed from the two already-rounded display values (0.00306 - 0.00282). From the unrounded slopes the excess is 0.000233, which rounds to 0.00023. The individual slopes quoted are correct; only the difference is off. The qualitative conclusion ("could account for most of the MSP narrowing", against an MSP gap slope of 0.000295) is unaffected: 0.000233/0.000295 = 79%.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/drift_slopes.csv'); m=d[d.score=='msp'].set_index('model').slope_per_week; print(m['ensemble'], m['member_mean'], abs(m['ensemble']-m['member_mean']))"
  -> -0.0030567709695785905 -0.0028242637100303995 0.000232507259548191
I also refit the fixed-effects slopes independently from results/exploratory/REVISION_FULL3/drift_members.csv (one intercept per start date, OLS on weeks_since) and reproduced every slope in Table \ref{tab:drift} to seven decimals, so the table itself is correct.
- **Suggested fix:** Change "an excess of $0.00024$" to "an excess of $0.00023$".

### [minor] paper/main.tex line 383 (Section V-B, "None of it comes from label agreement")
- **Paper says:** "\kd{hardA} is otherwise an ordinary student, matching the directly trained student's closed-set accuracy on the validation week ($0.913$ against $0.913$)"
- **Problem:** 0.913 is macro-F1, not accuracy. Validation accuracy on the standard flows=='all' basis is 0.941 for hardA and 0.941 for direct; macro-F1 is 0.9125 and 0.9131. The claim (that hardA is not a degenerate control) holds either way, but the metric is mislabelled -- and it contradicts Section V-G four pages later, which gives the same directly trained student's validation "closed-set accuracy" as 0.946.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; h=pd.read_csv('results/track_a_exploratory/20260922-130934_S_train11-14/metrics.csv'); h=h[(h.split=='val')&(h.flows=='all')&(h.unknown=='all')&(h.condition=='hardA')]; print(h[['accuracy','macro_f1']].mean())"
  -> accuracy 0.940593, macro_f1 0.912525
C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; m=pd.read_csv('results/track_a/20260919-113607_S_train11-14/metrics.csv'); m=m[(m.split=='val')&(m.flows=='all')&(m.unknown=='all')&(m.condition=='direct')]; print(m[['accuracy','macro_f1']].mean())"
  -> accuracy 0.941045, macro_f1 0.913124
- **Suggested fix:** Either relabel as macro-F1 ("matching the directly trained student's validation macro-F1, $0.913$ against $0.913$") or quote the accuracy ("$0.941$ against $0.941$"), and make the metric and basis consistent with Section V-G.

### [minor] paper/main.tex line 424 (Section V-G, "The arm is not degenerate")
- **Paper says:** "Its closed-set accuracy on the validation week is $0.945$ against the directly trained student's $0.946$, and its macro-F1 $0.918$ against $0.919$."
- **Problem:** These four values do not correspond to any single evaluation population. They are averages of the flows=='all' and flows=='ge5' rows -- two nested populations the rest of the paper never pools. On the paper's standard flows=='all' basis the numbers are 0.941 vs 0.941 (accuracy) and 0.912 vs 0.913 (macro-F1). Even under the mixed average, kdF's macro-F1 is 0.9175, which rounds to 0.917, not 0.918. The conclusion ("the feature penalty leaves the classification objective intact") is unaffected, but the reported figures are not reproducible from any stated basis and are inconsistent with Section V-B's 0.913.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; k=pd.read_csv('results/track_a_exploratory/20260923-051258_S_train11-14/metrics.csv'); k=k[(k.split=='val')&(k.condition=='kdF')]; print(k.groupby('flows')[['accuracy','macro_f1']].mean()); print('mixed mean', k[['accuracy','macro_f1']].mean().values)"
  -> flows all: accuracy 0.940685, macro_f1 0.912124
     flows ge5: accuracy 0.949674, macro_f1 0.922853
     mixed mean: [0.94517955 0.91748835]
Same for direct (results/track_a/20260919-113607_S_train11-14/metrics.csv):
  -> flows all: accuracy 0.941045, macro_f1 0.913124
     flows ge5: accuracy 0.950083, macro_f1 0.924381
     mixed mean: [0.945564 0.918753]
- **Suggested fix:** Quote the flows=='all' values used elsewhere in the paper: "Its closed-set accuracy on the validation week is $0.941$ against the directly trained student's $0.941$, and its macro-F1 $0.912$ against $0.913$."

### [minor] paper/main.tex line 426 (Section V-G, "What it does not do is transfer the teacher's advantage")
- **Paper says:** "Over the nine windows start date 11 tests, \kd{kdF} gains $+0.014$ Mahalanobis AUROC over \kd{direct} ... and $+0.018$ under feature $k$-NN ..., against the $+0.073$ the teacher has available."
- **Problem:** The sentence is explicit that kdF's estimates come from nine windows, then benchmarks them against a teacher advantage pooled over all eighteen. On the same nine windows the teacher's advantage is +0.0835 Mahalanobis and +0.0373 feature k-NN. The mismatch happens to work against the paper's own argument (kdF recovers 16% of the matched benchmark, not 19%), so the conclusion is if anything strengthened -- but the two quantities as written are computed over different unit sets, which is exactly the comparison the section's own caution warns about.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "...concat results/feature_scores/*_S_train*/metrics.csv, test splits, flows=='all', unknown=='all'; per (start,split) mean of teacherA minus direct..."  ->
  Mahalanobis: mean over all 18 units = +0.0727 (matches the reported 0.073); mean over the 9 start-11 units = +0.0835
  feature k-NN: mean over 18 = +0.0338; mean over the 9 start-11 units = +0.0373
- **Suggested fix:** Use the matched benchmark: "...against the $+0.084$ the teacher has available on those same nine windows" (and $+0.037$ for feature k-NN), or state explicitly that $+0.073$ is the 18-window pooled figure.

### [nit] paper/main.tex line 424 (Section V-G, last sentence)
- **Paper says:** "Nor is the penalty inert: \kd{kdF} ends training at a loss of $0.257$ where \kd{direct} reaches $0.182$."
- **Problem:** Both figures are the seed-0 values. Averaged over the three seeds the paper uses everywhere else, the final training losses are 0.256 for kdF and 0.183 for direct. The point being made is unaffected; the numbers are just not on the paper's usual seed-averaged basis.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import json,numpy as np; t=json.load(open('results/track_a_exploratory/20260923-051258_S_train11-14/training.json')); v=[t[n]['history'][-1]['train_loss'] for n in sorted(t) if n.startswith('student_kdF_s')]; print(v, np.mean(v))"
  -> [0.2567, 0.2543, 0.2579] mean 0.25631
C:/venvs/kd-traffic/Scripts/python.exe -c "...same for results/track_a/20260919-113607_S_train11-14/training.json, student_direct_s*..."
  -> [0.1823, 0.1819, 0.1841] mean 0.18276
- **Suggested fix:** Use the seed means: "ends training at a loss of $0.256$ where \kd{direct} reaches $0.183$" (both averaged over three seeds).

## prereg-integrity  (7 raised; 11/21 skeptic votes refuted)

### [major] paper/main.tex line 464 (Reproducibility); also line 87 and docs/preregistration.md header table
- **Paper says:** "with the commit that froze the pre-registration tagged \texttt{prereg-frozen} so that the registered protocol and the analysis code implementing it can be checked against each other" (line 464); "frozen 19 September 2026 at repository commit \texttt{fa8f718}" (line 87)
- **Problem:** The tag points one commit too early. `prereg-frozen` resolves to fa8f718, whose copy of docs/preregistration.md still reads "**Status:** DRAFT (not frozen)" with placeholder rows for Frozen on / Git commit / OSF registration. The commit that actually froze the document is its child, 55e0829 ("FREEZE the pre-registration"), which flips the status line and fills the header table — and it is that flip that opens the guard in 06_track_a.py / 08_analyze.py. A reviewer who checks out the tag to do exactly what the sentence invites (compare the registered protocol against the analysis code) finds a document labelled DRAFT and no registered protocol. The substance is fine (the protocol text at fa8f718 is identical to the frozen text apart from the header, and no test window was touched before 55e0829), but the artefact the paper points at contradicts the paper's central rigour claim.
- **Evidence:** `git rev-list -n1 prereg-frozen` -> fa8f718002b90e23b91e7f4bec463015c4281fc7. `git tag -n20 prereg-frozen` -> "Pre-registration frozen (docs/preregistration.md); OSF https://osf.io/rts6n". `git show fa8f718:docs/preregistration.md | head -14` -> "**Status:** DRAFT (not frozen)", "| Frozen on | *(date)* | | Git commit | *(hash)* | | OSF registration | *(link)* |". `git log --format="%h %ad %s" --date=iso 55e0829 -1` -> "55e0829 2026-09-19 11:34:15 +0200 FREEZE the pre-registration (decisions D1-D8 settled on validation data only)"; `git rev-parse 55e0829^` -> fa8f718. `git diff fa8f718 55e0829 -- docs/preregistration.md` changes only lines 1-14 (status line + header table). First confirmatory run directory is results/track_a/20260919-113607_S_train11-14, i.e. 11:36, two minutes after the freeze commit.
- **Suggested fix:** Move the tag to 55e0829 (`git tag -f prereg-frozen 55e0829 && git push --force origin prereg-frozen`) and change line 87 to "frozen 19 September 2026 at repository commit \texttt{55e0829}"; or, if the tag must stay where the pre-registration header points, reword line 464 to "with the code state at the freeze tagged \texttt{prereg-frozen} (the status line is flipped in its child commit \texttt{55e0829})" and add a dated §11 entry explaining the two-commit convention.

### [major] paper/main.tex, Section IV-E "Baselines and Deployment Cost" (lines 335-339), inside \section{Confirmatory Results} (line 176)
- **Paper says:** Section IV is headed "Confirmatory Results" and opens "All numbers come from the pre-registered test windows unless marked otherwise." Section V opens "The analyses in this section were added after the confirmatory results were known." Pre-registration §9 heads its list "Exploratory analyses (labelled as such in the paper)" and lists "XGBoost and k-NN baselines" and "CPU deployment measurements". §11 item 7 lists "XGBoost re-tuned to a deployable size" among the peer-review additions, which it says are "reported under its own heading in the paper".
- **Problem:** Section IV-E carries no exploratory label, yet everything in it is exploratory by the pre-registration's own classification, and part of it is post-hoc. The baselines and the CPU measurements are pre-registered §9 exploratory analyses that §9 requires to be labelled as such; the re-tuned XGBoost (0.854 / 0.826, 73 MB, 14.4 ms) and its re-timing session were added at peer review after the confirmatory results were known, and are logged in §11 as exploratory. Placing them under "Confirmatory Results" contradicts both §9's labelling instruction and the paper's own statement that Section V contains everything added after the confirmatory results were known. This is the one place in the manuscript where the confirmatory/exploratory boundary the paper sells is actually crossed.
- **Evidence:** Run dates: `ls results/baselines/` -> 20260916-134400 (original, pre-freeze) and 20260923-030958 (re-tuned); `ls results/cpu_cost/` -> 20260920-014955 and 20260923-031218. Confirmatory analysis commit 7ef3b5d is dated 2026-09-19 19:06:46; the freeze is 2026-09-19. So both the re-tuned XGBoost (23 Sep) and both CPU sessions (20 and 23 Sep) postdate the confirmatory results. docs/preregistration.md §9 line 183 "## 9. Exploratory analyses (labelled as such in the paper)" with bullets "- XGBoost and k-NN baselines." and "- CPU deployment measurements."; §11 line 218 "7. **XGBoost re-tuned** to a deployable size, reported beside the original configuration." and line 207 "it is reported under its own heading in the paper".
- **Suggested fix:** Either move Section IV-E to Section V (as e.g. "V-A Baselines and Deployment Cost"), or keep it where it is and open it with one sentence: "This subsection is exploratory: the baselines and the CPU measurements are listed as exploratory analyses in the pre-registration (§9), and the re-tuned XGBoost configuration was added at peer review, after the confirmatory results were known (§11, item 7)."

### [minor] paper/response-to-reviewers.md, answer to concern 7, lines 184-186
- **Paper says:** "We now say this in three places: Section III-C, which describes the tuning pass and why the arm was added; the paragraph introducing Table III, which repeats it where the H1 result is read; and §11 of the pre-registration."
- **Problem:** §11 of the pre-registration says nothing about when or why the T=4 arm was added. §11 contains four dated entries (OSF registration, the timing note, the 22 Sep exploratory additions, the 23 Sep feature-distillation entry); its only mention of T=4 is in the description of the figure change in commit 1d79a0c ("it adds the T=4 arm to the specificity plot"). The arm's provenance is recorded in decision D8 in §4, which is above §11 and part of the frozen body — correctly so, since the arm was added before the freeze and therefore is not a deviation. The response letter directs the reviewer to a section where the answer is not, on the one point where the reviewer questioned the pre-registration's integrity.
- **Evidence:** python: reading docs/preregistration.md, splitting on '## 11. Deviations', regex `T\s*=\s*4` over the §11 text returns exactly one match, which is the sentence "it adds the T=4 arm to the specificity plot and adjusts bar order, figure size, axis limits and legend placement"; `s.count('kdA4')` over §11 returns 1 (the paired `ls`-against-`kdA4` row). Git confirms the arm predates the freeze: `git log` -> b48b378 2026-09-17 23:51:24 "Add the conventional-KD arm (kdA4/kdB4 at T=4) for H1 and the shortcut experiment", freeze 55e0829 2026-09-19 11:34:15.
- **Suggested fix:** Change "and §11 of the pre-registration" to "and decision D8 in §4 of the pre-registration, settled 17 September and frozen with the rest of the document". (Same document, line 176, also cites "Section VII" for the limitations paragraph while line 186 cites "Section VI"; the limitations paragraph is in Section VI, Discussion.)

### [minor] docs/preregistration.md §11 (whole log), against §8 (Track B) and §9 (planned exploratory analyses)
- **Paper says:** §5/header: "From now on it changes only by appending dated entries to section 11 (Deviations)." §8: "**Track B** (netFound): continue only if preprocessing works within one week and netFound-small beats XGBoost by >=2 macro-F1 points; otherwise it becomes an appendix." §9 plans, among others, "The natural-emergence services" and "Conformal recalibration with delayed labels".
- **Problem:** §11 records every analysis that was *added* after the freeze, with exemplary precision, but records no analysis that was *dropped*. Three registered items were never carried out and are never mentioned in the paper: Track B (netFound) has no appendix and no sentence explaining why it was abandoned; conformal recalibration with delayed labels is absent; and the natural-emergence set is absent although configs/splits.json permanently reserves seven services for it, removing them from the known set. The paper's Data section describes the 46 excluded services as excluded "mostly for insufficient or inconsistent volume" without noting that seven of them are held back for an analysis that was not run. A reader comparing the OSF registration with the paper will find three registered items silently missing; the standard practice a pre-registered paper is judged by is to log the omissions as well as the additions.
- **Evidence:** `grep -rn -i "track b|netfound|conformal" STATUS.md docs/preregistration.md paper/main.tex paper/response-to-reviewers.md` -> the only hits in paper/main.tex are the \cite{netfound} in the introduction; the only other hits are docs/preregistration.md:181 (Track B rule) and :187 (conformal), and STATUS.md:74 "**Still to write:** Track B (netFound on UNSW-IoTraffic); ... conformal recalibration (exploratory)." python over configs/splits.json: key `emerging_w11_14` -> 7 services ['amazon-alexa','cesnet-kalendar','cesnet-login','cesnet-perun','font-awesome','gitlab','redmine'], each with disabled_reason "emerging after weeks 11-14 (reserved for the natural-emergence test)"; `grep -c "natural-emergence" paper/main.tex` -> 0.
- **Suggested fix:** Append a dated §11 entry: "24 Sep 2026 - registered analyses not carried out. Track B (netFound, §8) was not attempted; the preprocessing budget was spent elsewhere. The natural-emergence supplementary set (§3, §9) and conformal recalibration with delayed labels (§9) were not run. All three are exploratory or conditional; no confirmatory hypothesis is affected." and add one clause to the paper's Data paragraph noting that seven of the 46 excluded services were reserved for a natural-emergence analysis that this paper does not report.

### [minor] paper/main.tex line 235
- **Paper says:** "The two sensitivity analyses (exact duplicates of training flows removed, 1.8\% of test flows; flows with at least five packets only, 94.0\% of test flows) reach the same decision on every hypothesis, with every $p$-value within 0.1 of the primary analysis."
- **Problem:** Neither sensitivity analysis was computed for three of the ten hypotheses. H4a, H4b1 and H4b2 run on the shortcut experiment's validation week, where a training-duplicate flag and a test-window packet filter do not apply, so components.csv has those three rows only under analysis=='primary'. The statement as written tells the reader that all ten hypotheses survived both robustness checks; seven did. (For those seven the claim is exactly right at the hypothesis level: the largest shift in an IUT p-value is H3[energy], 0.993 -> 0.927.)
- **Evidence:** python: `c=pd.read_csv('results/analysis/CONFIRMATORY/components.csv'); c.pivot_table(index=['hypothesis','component'],columns='analysis',values='p_one_sided')` -> rows H4a/reliance_kd_minus_direct, H4b1/ece_minus_rho0, H4b2/rho0_minus_auroc have NaN under both `drop_duplicates` and `ge5_packets`; all other rows are populated under all three. Max hypothesis-level |p difference| over the seven test-window hypotheses = 0.0659 (H3[energy]).
- **Suggested fix:** "...reach the same decision on every test-window hypothesis, with every $p$-value within 0.1 of the primary analysis; they do not apply to H4a, H4b1 and H4b2, which are measured on the shortcut experiment's validation week."

### [minor] paper/main.tex line 344 (Section V opening) and Fig. 5 caption (line 358); docs/preregistration.md §11, last paragraph of the 22 Sep entry
- **Paper says:** "Intervals come from the same day$\times$service cluster bootstrap, resampling training seeds in the same way, so that they can be read against the confirmatory intervals." Fig. 5 caption: "bars are 95\% cluster bootstrap intervals". §11: "Exploratory intervals are built by the same day x service cluster bootstrap as the confirmatory ones ... so that the two can be compared directly."
- **Problem:** The exploratory runs use 300 bootstrap resamples; the confirmatory analysis uses 1,000. The paper states the confirmatory count twice (Section III-F and the Table III caption, including the resulting 1/1001 p-value floor) but never states the exploratory count, while explicitly inviting the reader to read the two sets of intervals against each other. A 3.3-fold reduction in resamples makes the exploratory percentile intervals materially coarser and raises the one-sided p-value floor to 1/301 = 0.0033 (visible in detection_advantage.csv), which is the value several exploratory rows report.
- **Evidence:** `cat results/exploratory/REVISION_FULL3/config.json` -> "n_boot": 300. `cat results/analysis/CONFIRMATORY/config.json` -> "n_boot": 1000. In results/exploratory/REVISION_FULL3/detection_advantage.csv every row has n_boot=300 and the smallest p_one_sided is 0.003322 (= 1/301).
- **Suggested fix:** Add to line 344: "...resampling training seeds in the same way, at 300 resamples rather than the confirmatory 1,000, so that they can be read against the confirmatory intervals while being somewhat coarser." Ideally re-run 12_exploratory.py at n_boot=1000 so the two match.

### [minor] paper/main.tex line 374 (Section V-A, last paragraph)
- **Paper says:** "the ensemble-distribution-distillation student, the worst detector in the grid under the energy score ($-0.092$ against \kd{kdA}, CI $-0.100$ to $-0.085$)"
- **Problem:** The confidence interval is taken from the wrong row. In the exploratory table, `enddA - kdA` under the energy score is -0.092009 with CI (-0.099485, -0.084715), i.e. -0.099 to -0.085; the interval -0.100 to -0.085 belongs to `enddA - direct student` (-0.092712, CI -0.100440 to -0.085180), a different comparison. The quoted lower bound also disagrees with Table III's confirmatory interval for the same quantity (-0.099, -0.084), so a reader cross-checking the exploratory restatement against the confirmatory table finds a mismatch that has no explanation.
- **Evidence:** python over results/exploratory/REVISION_FULL3/detection_advantage.csv: row (score='energy', comparison='enddA - kdA') -> estimate -0.092009, ci_low -0.099485, ci_high -0.084715; row (score='energy', comparison='enddA - direct student') -> estimate -0.092712, ci_low -0.100440, ci_high -0.085180. Same values in REVISION_FULL and REVISION_FULL2. results/analysis/CONFIRMATORY/components.csv, H5[energy]/auroc_enddA_minus_kdA -> estimate -0.0920090628, ci (-0.0992369545, -0.0843676144).
- **Suggested fix:** Change to "($-0.092$ against \kd{kdA}, CI $-0.099$ to $-0.085$)", or drop the interval here and refer to Table III, since this is a restatement of a confirmatory estimate.

## reproducibility-claims  (6 raised; 11/18 skeptic votes refuted)

### [major] paper/refs.bib:4 (entry `luxemburk2024year22`); rendered as reference [1] in dist/arxiv-20260924.tar.gz → main.bbl. Same wrong list at LICENSE-DATA:23 and deposit/README.md:10 (the latter already published on Zenodo).
- **Paper says:** `author  = {Luxemburk, Jan and Ple{\v{s}}n{\'\i}k, Karel and Hynek, Karel}` — rendering as "J. Luxemburk, K. Plešník, and K. Hynek, ``{CESNET-TLS-Year22}: A year-spanning {TLS} network traffic dataset from backbone lines,'' \emph{Scientific Data}, vol.~11, 2024."
- **Problem:** The author list for the dataset the entire study is built on is wrong. The real paper (DOI 10.1038/s41597-024-03927-4, the DOI already in the entry) has five authors led by Karel Hynek, not three led by Jan Luxemburk; "Karel Plešník" is not an author of it at all, and Jaroslav Pešek, Tomáš Čejka and Pavel Šiška are omitted. Three independent sources agree (publisher meta tags, Crossref, OpenAlex). This is reference [1] of a paper about to be posted permanently, it is the citation the paper's own text points at when it says "the dataset authors", and the same wrong attribution is in the LICENSE-DATA file of the public repository and in the README already served from the Zenodo record. The article number (1156) is also missing.
- **Evidence:** `C:/venvs/kd-traffic/Scripts/python.exe -c "import requests,re; r=requests.get('https://www.nature.com/articles/s41597-024-03927-4',headers={'User-Agent':'Mozilla/5.0'},timeout=90); print(re.findall(r'name=\"citation_author\" content=\"([^\"]+)\"', r.text))"` → `['Hynek, Karel', 'Luxemburk, Jan', 'Pe&#353;ek, Jaroslav', '&#268;ejka, Tom&#225;&#353;', '&#352;i&#353;ka, Pavel']`, with `citation_volume=11`, `citation_firstpage=1156`. Crossref (`https://api.crossref.org/works/10.1038/s41597-024-03927-4`) returns the same five in the same order with `sequence=first` on Hynek; OpenAlex (`https://api.openalex.org/works/doi:10.1038/s41597-024-03927-4`) returns `first Karel Hynek; middle Jan Luxemburk; middle Jaroslav Pešek; middle Tomáš Čejka; last Pavel Šiška`. Title returned by all three is byte-for-byte the title in the entry, so it is the same paper.
- **Suggested fix:** Set `author = {Hynek, Karel and Luxemburk, Jan and Pe{\v{s}}ek, Jaroslav and {\v{C}}ejka, Tom{\'a}{\v{s}} and {\v{S}}i{\v{s}}ka, Pavel}` and add `pages = {1156}` in paper/refs.bib; rebuild main.bbl and the dist/ arXiv bundle (the bundle currently ships the wrong .bbl). Fix the same sentence in LICENSE-DATA:23 and deposit/README.md:10, and re-upload the corrected README to the Zenodo record (a metadata/file edit on the published version, or note it in a v2).

### [minor] paper/main.tex:21 (author \thanks) vs paper/main.tex:474 (IEEEbiographynophoto)
- **Paper says:** Line 21: "M. Abbasi is with the University of Salamanca, Salamanca, Spain". Line 474: "Since 2026, Dr.\ Abbasi has been a Researcher with the AIR Institute, Salamanca, Spain."
- **Problem:** The two present-tense affiliation statements in the same paper disagree. The paper is dated September 2026, so by its own biography the current employer is the AIR Institute, while the IEEE author footnote — the field readers and indexers take as the current affiliation — says University of Salamanca. The Zenodo deposit records only "University of Salamanca", so whichever is right, one of the three is out of date.
- **Evidence:** `awk 'NR>=20 && NR<=22' paper/main.tex` → `\thanks{M. Abbasi is with the University of Salamanca, Salamanca, Spain (e-mail: mahmoud.abbasi@ieee.org; ORCID 0000-0002-1886-8284).}`. `awk 'NR==474' paper/main.tex` → `Since 2026, Dr.\ Abbasi has been a Researcher with the AIR Institute, Salamanca, Spain.` Zenodo API `metadata.creators` → `[{"name": "Abbasi, Mahmoud", "affiliation": "University of Salamanca", "orcid": "0000-0002-1886-8284"}]`.
- **Suggested fix:** Make the \thanks match the biography, e.g. "M. Abbasi is with the AIR Institute, Salamanca, Spain", or list both ("with the AIR Institute and the University of Salamanca"), and align the Zenodo creator affiliation with whichever is chosen.

### [minor] deposit/README.md:31-42 ("Reading a score file"), published as README.md on the Zenodo record
- **Paper says:** "Each `scores_<split>.npz` holds one array per model, in the flow order of that evaluation window, plus the arrays needed to evaluate them" — the example then shows only `y` and `day` — and "Score suffixes are `energy`, `msp`, and, in the feature-space archive, `maha` and `knnfeat`."
- **Problem:** The description is incomplete in a way that matters for reproducing results the paper actually reports. The deposited files also contain `app`, `ppi_len` and `duplicate`, and the suffixes `msp_ts`, `nll_ts` and `pred`. `ppi_len` and `duplicate` are precisely the arrays needed to reproduce the two pre-registered sensitivity analyses (`ge5_packets` and `drop_duplicates` in units.csv), `msp_ts`/`nll_ts` carry the post-temperature-scaling calibration numbers, and `pred` is needed for macro-F1 and OSCR. A reader following the README would conclude those quantities are not in the deposit and would have to retrain to get them.
- **Evidence:** `C:/venvs/kd-traffic/Scripts/python.exe -c "import numpy as np; z=np.load('results/track_a/20260919-113607_S_train11-14/scores_test_w16-19.npz'); k=list(z.keys()); print([x for x in k if '__' not in x]); print(sorted(set(x.split('__')[1] for x in k if '__' in x)))"` → `['y', 'app', 'ppi_len', 'duplicate', 'day']` and `['energy', 'msp', 'msp_ts', 'nll_ts', 'pred']`. (The file is manifest row `scores-confirmatory`, so this is a deposited file, not a local extra.)
- **Suggested fix:** Extend the README's key list to `y, app, ppi_len, duplicate, day` with one line each on what `ppi_len` and `duplicate` are for, and extend the suffix list to `energy, msp, msp_ts, nll_ts, pred` (plus `maha`, `knnfeat` in the feature-space archive). Re-upload the README to Zenodo.

### [nit] paper/main.tex:466 (Reproducibility) and deposit/README.md:22
- **Paper says:** "...and the 206 model checkpoints exceed what a source repository can hold"; deposit README table row "`checkpoints.zip` | Trained teacher and student weights | 206 | 356 MB".
- **Problem:** 206 is the file count, but 108 of those 206 files are exact byte-duplicates of one another (54 pairs with identical SHA-256, each pair being the same weights stored under both the original run directory and its `--with-test` re-run). The deposit therefore contains 152 distinct trained models. The file count is literally true, but a reader sizing up the deposit will read "206 model checkpoints" as 206 models.
- **Evidence:** `C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; m=pd.read_csv('deposit/MANIFEST.csv'); c=m[m.archive=='checkpoints']; print(len(c), c.sha256.nunique(), c.duplicated('sha256',keep=False).sum())"` → `206 152 108`. Example pair: `results/track_a/20260919-113607_S_train11-14/models/student_kdB4_s0.pt` and `results/track_a/20260918-121700_S_train11-14/models/student_kdB4_s0.pt`, both 418291 bytes, identical digest.
- **Suggested fix:** Either say "206 checkpoint files (152 distinct models; the `--with-test` re-runs reuse the same weights)", or drop the duplicate paths from the archive and update the count. No change is needed if the redundancy is deliberate — the manifest digests already make it discoverable.

### [nit] paper/main.tex:465-466 (Reproducibility)
- **Paper says:** "The per-flow unknown-scores for every model and evaluation window (129 files, 8.2\,GB) and the 206 model checkpoints... are deposited at Zenodo"
- **Problem:** "every model" is broader than what is deposited. The deposit covers `track_a`, `track_a_exploratory`, `feature_scores` and the `pilot` teachers only. The shortcut experiment (Section on shortcut reliance) has neither per-flow scores nor checkpoints in the deposit — `scripts/07_shortcut.py:106` does call `torch.save`, but no `.pt` survives under `results/shortcut/`, and no shortcut path appears in the manifest. The XGBoost/kNN baselines likewise have no deposited artefact. Those results can be re-run but not re-derived from the deposit.
- **Evidence:** `C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; m=pd.read_csv('deposit/MANIFEST.csv'); print(m.path.str.split('/').str[1].value_counts())"` → `track_a 181, track_a_exploratory 107, feature_scores 41, pilot 6` (no `shortcut`, no `baselines`). `find results/shortcut -name '*.pt'` → no output; `ls results/shortcut/20260918-234156_S_train11-14/` → `config.json  log.txt  reliance.csv  shortcut.csv`.
- **Suggested fix:** Narrow the phrase, e.g. "The per-flow unknown-scores for every model in the condition grid, at every evaluation window (129 files, 8.2 GB)...", and optionally add one sentence that the shortcut experiment and the baselines are reproduced by re-running `scripts/07_shortcut.py` and `scripts/04_baselines.py` from the released code.

### [nit] paper/refs.bib:43 (`netfound`), :12 (`luxemburk2022reject`), :130 (`teacherspet`)
- **Paper says:** `netfound`: `author = {Guthula, Satyandra and Beltiukov, Roman and Battula, Navya and Guo, Wenbo and Gupta, Arpit}`. `luxemburk2022reject` and `teacherspet` are `@inproceedings` with `booktitle = {Computer Networks}` and `booktitle = {Transactions on Machine Learning Research}`.
- **Problem:** Two small bibliography slips. (a) The netFound author order swaps the 2nd and 3rd authors relative to arXiv v1, which the entry otherwise reproduces exactly. (b) Computer Networks and TMLR are journals, but the `@inproceedings` type makes IEEEtran typeset them as conference proceedings: the .bbl currently reads "in \emph{Computer Networks}, 2023" and "in \emph{Transactions on Machine Learning Research}, 2022".
- **Evidence:** arXiv API `id_list=2310.17025v1` → title `netFound: Foundation Model for Network Security`, authors `Satyandra Guthula; Navya Battula; Roman Beltiukov; Wenbo Guo; Arpit Gupta` (Battula before Beltiukov). Rendered .bbl from the arXiv bundle: `J.~Luxemburk and T.~{\v{C}}ejka, ``Fine-grained {TLS} services classification with reject option,'' in \emph{Computer Networks}, 2023, arXiv:2202.11984.` and `M.~Lukasik, ... ``Teacher's pet: ...,'' in \emph{Transactions on Machine Learning Research}, 2022.` All 41 arXiv identifiers in refs.bib resolve and every other first author and author count matches the arXiv record.
- **Suggested fix:** Swap Battula and Beltiukov in the `netfound` entry (and consider noting it is v1, since arXiv now shows a retitled 8-author version). Change `luxemburk2022reject` and `teacherspet` to `@article` with `journal = {...}`.

## statistics  (11 raised; 14/32 skeptic votes refuted)

### [major] paper/main.tex line 235 (Section IV-A), and line 174 (Section III-E)
- **Paper says:** "The two sensitivity analyses (exact duplicates of training flows removed, 1.8\% of test flows; flows with at least five packets only, 94.0\% of test flows) reach the same decision on every hypothesis, with every $p$-value within 0.1 of the primary analysis."  Section III-E: "Two sensitivity analyses repeat everything with exact duplicates of training flows removed and with flows of fewer than five packets removed".
- **Problem:** The sensitivity analyses cover only 7 of the 10 hypotheses. H4a, H4b1 and H4b2 (the shortcut experiment) are never recomputed under either filter -- the shortcut run is on the validation week and `scripts/08_analyze.py` attaches its components to the primary analysis only. "Repeat everything" and "the same decision on every hypothesis" therefore overstate the coverage, including for H4b1, one of the paper's two supported hypotheses.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; c=pd.read_csv('results/analysis/CONFIRMATORY/components.csv'); t=c[c.hypothesis!='info']; [print(a, sorted(t[t.analysis==a].hypothesis.unique())) for a in c.analysis.unique()]"  ->  primary: 10 hypotheses (H1, H2[energy], H2[msp], H3[energy], H3[msp], H4a, H4b1, H4b2, H5[energy], H5[msp]); drop_duplicates: 7 (no H4a/H4b1/H4b2); ge5_packets: 7 (no H4a/H4b1/H4b2).  Cause: scripts/08_analyze.py line 244, `components.append(shortcut_components(reliance, shortcut).assign(analysis="primary"))` -- computed once, outside the per-analysis loop at lines 231-238.
- **Suggested fix:** In III-E write "repeat the seven flow-level hypotheses" (the shortcut experiment is on the validation week and is not affected by test-flow filters); in IV-A write "reach the same decision on each of the seven hypotheses they cover, with every $p$-value within 0.1 of the primary analysis; the shortcut hypotheses (H4a, H4b1, H4b2) are measured on the validation week and are not re-run."

### [major] paper/main.tex line 235 (Section IV-A); bears on Section IV-B line 279, contribution 3 at line 56, and the abstract at line 29
- **Paper says:** "The two sensitivity analyses ... reach the same decision on every hypothesis" -- offered as the robustness check behind, among others, the reversed H3 result ("The drift prediction is reversed under both scores").
- **Problem:** True but materially incomplete for the headline reversal. "The same decision" is trivially preserved because the registered decision is "not supported" under every analysis. The *reversal* -- the actual claim the paper makes and builds contribution 3 on -- is not robust: under the ge5_packets sensitivity analysis the H3[energy] slope shrinks by 42% and its 95% interval covers zero. The energy score is precisely the score on which the paper claims the student overtakes the teacher. A reader is given a blanket reassurance that does not extend to the result it is most needed for.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; c=pd.read_csv('results/analysis/CONFIRMATORY/components.csv'); print(c[(c.hypothesis=='H3[energy]')][['analysis','estimate','ci_low','ci_high','p_one_sided']].to_string())"  ->  primary: -0.000381 (-0.000685, -0.000077); drop_duplicates: -0.000330 (-0.000623, -0.000047); ge5_packets: -0.000222 (-0.000534, +0.0000777)  <- interval includes zero.  (H3[msp] excludes zero in all three: ge5_packets -0.000176 (-0.000334, -0.0000119).)
- **Suggested fix:** Add one clause, e.g. "...reach the same decision on every hypothesis they cover. The reversal of H3 is likewise reproduced, except that under the $\ge5$-packet analysis the energy-score slope falls to $-0.00022$ with an interval spanning zero ($-0.00053$ to $+0.00008$); the MSP reversal holds in both sensitivity analyses." This is honest and costs the argument little, since the crossover in Table V and the three per-start slopes are independent of it.

### [major] paper/main.tex line 29 (abstract), line 57 (contribution 4), line 308 (Section IV-C), line 446 (Discussion)
- **Paper says:** Abstract: "Shortcut reliance is set by model size, not distillation."  Contribution 4: "distillation does not increase that reliance."  Section IV-C: "Distillation does not increase shortcut reliance."
- **Problem:** A "not supported" one-sided test over six pairs is being read as evidence of absence, and the pooled sign is an artefact of stratum heterogeneity. The six (rho, seed) differences split cleanly by rho: all three at rho=0.9 are POSITIVE (the KD student relies MORE, +0.007 to +0.015), all three at rho=1.0 are negative (-0.068 to -0.085, where the direct student saturates at 0.95 macro-F1 lost). The negative pooled mean is entirely the rho=1 stratum. The two-sided 95% interval on the pooled difference is (-0.082, +0.018), so an increase as large as the one actually observed at rho=0.9 is not excluded. Section IV-C does disclose that the difference is "driven by $\rho=1$", but the abstract, the contribution bullet and the discussion state the null flatly.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; from scipy.stats import ttest_1samp; r=pd.read_csv('results/shortcut/20260918-234156_S_train11-14/reliance.csv'); w=r[r.rho.astype(float).isin([0.9,1.0])].pivot_table(index=['rho','seed'],columns='model',values='reliance_f1_drop'); d=w['student_both_kd']-w['student_both_direct']; print(d.to_string()); print(ttest_1samp(d,0))"  ->  rho=0.9: +0.011817, +0.015027, +0.006585;  rho=1.0: -0.067727, -0.084858, -0.072218.  Two-sided t-test: p = 0.1613, 95% CI (-0.0818, +0.0180). One-sided (registered) p = 0.9193, lower bound -0.0710.
- **Suggested fix:** Soften the three generalising statements to match Section IV-C's own hedge, e.g. abstract: "Shortcut reliance tracks model size; the pre-registered test that distillation increases it is not supported, though the direction depends on $\rho$."  Contribution 4: "...and the pre-registered test finds no increase from distillation (the pooled estimate is negative, driven by $\rho=1$; at $\rho=0.9$ the KD student relies slightly more in all three seeds)."

### [minor] paper/main.tex line 417 (Section V-F, Student Capacity)
- **Paper says:** "Distillation does not close the gap at any width. \kd{kdA4} is worse than the equally sized directly trained student at width 16 ($-0.054$ against \kd{direct}) and at width 96 ($-0.016$), under the energy score."
- **Problem:** The quoted numbers are not the comparison the sentence describes. Every row of the exploratory table is measured against the registered width-48 `direct` student, not against a width-matched one (`scripts/12_exploratory.py` line 218 calls `detection_shift(u, c, "direct", ...)` for all conditions including the width arms). Against the *equally sized* direct student the width-16 gap is -0.030, not -0.054 -- the quoted figure is ~1.8x too large -- and the width-96 gap is -0.017, not -0.016. The conclusion's direction survives, but the magnitude a reader takes away does not.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[(d.score=='energy')&d.comparison.str.contains('w16|w96')][['comparison','estimate']].to_string())"  ->  direct_w16 - direct student: -0.024023;  direct_w96 - direct student: +0.001584;  kdA4_w16 - direct student: -0.053771;  kdA4_w96 - direct student: -0.015744.  Width-matched differences: -0.053771-(-0.024023) = -0.0298;  -0.015744-0.001584 = -0.0173.
- **Suggested fix:** Either re-state the baseline explicitly -- "$-0.054$ against the registered width-48 \kd{direct}, or $-0.030$ against the width-16 \kd{direct}" -- or bootstrap the width-matched paired difference the way `ls - kdA4` is bootstrapped, and quote that.

### [minor] paper/main.tex line 233 (Section IV, reading conventions for Table III)
- **Paper says:** "With 1,000 resamples the smallest attainable one-sided $p$ is $1/1001 = 0.000999$, so 0.009 is the smallest Holm-corrected value a family of ten can produce, and rows shown as 1.0 are bounded rather than precisely measured."
- **Problem:** Not correct, and contradicted by the table it explains. A floor-valued bootstrap $p$ ranked first in a family of ten is multiplied by 10, giving 0.00999, not 0.009. H1 receives 0.009 only because it ranks *second*: H4b1's $p$ comes from a $t$-test, is smaller than the bootstrap floor, and takes the multiplier of 10. And the same table's H4b1 row shows a Holm value of 0.005, which is below the 0.009 the sentence calls the minimum.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; from kdtraffic.analysis import holm; c=pd.read_csv('results/analysis/CONFIRMATORY/components.csv'); t=c[(c.analysis=='primary')&(c.hypothesis!='info')]; p=t.groupby('hypothesis').p_one_sided.max().to_dict(); h=holm(p); [print(k, round(p[k],6), round(h[k],6)) for k in sorted(p)]"  ->  H4b1 raw 0.000473 -> Holm 0.004731 (rank 1, x10);  H1 raw 0.000999 -> Holm 0.008991 (rank 2, x9).  Had H1 ranked first it would have been 10 x 0.000999 = 0.00999.
- **Suggested fix:** "With 1,000 resamples the smallest attainable one-sided $p$ is $1/1001=0.000999$, so a bootstrap hypothesis cannot fall below about 0.01 after Holm correction; H1's 0.009 is that floor, one rank down, because H4b1's $t$-test $p$ is smaller still. Rows shown as 1.0 are bounded rather than precisely measured."

### [minor] paper/main.tex line 344 (Section V preamble)
- **Paper says:** "Intervals come from the same day$\times$service cluster bootstrap, resampling training seeds in the same way, so that they can be read against the confirmatory intervals."
- **Problem:** The resampling *scheme* is the same, but the number of resamples is not, and the paper never says so. The exploratory run used 300 resamples against the confirmatory 1,000. The sentence explicitly invites the reader to read the two sets of intervals against each other, and the caption of Table III states 1,000, so the natural inference is that the exploratory intervals also rest on 1,000. A 300-draw percentile interval is visibly noisier (the H1 replication row gives (+0.0046, +0.0091) where the confirmatory run gives (0.0048, 0.0090) for the identical statistic), and the exploratory one-sided p-value floor is 1/301 = 0.0033 rather than 1/1001.
- **Evidence:** cat results/exploratory/REVISION_FULL3/config.json  ->  "n_boot": 300.  C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; print(pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv').n_boot.unique(), pd.read_csv('results/exploratory/REVISION_FULL3/swaps.csv').p_one_sided.min())"  ->  [300]  0.0033223 (= 1/301).  Confirmatory: components.csv n_boot column is 1000 throughout.
- **Suggested fix:** Add the count: "...the same day$\times$service cluster bootstrap, resampling training seeds in the same way, at 300 resamples rather than the confirmatory 1,000, so that they can be read against the confirmatory intervals." Also state the 300 in the captions of Tables VIII and IX.

### [minor] paper/main.tex line 344 (Section V preamble)
- **Paper says:** "None of them is pre-registered, none shares a multiple-comparison family with the ten hypotheses of Table~\ref{tab:hypotheses}, and none alters a number reported above."
- **Problem:** This correctly excludes the exploratory results from the confirmatory family but never says what the reader most needs to know: that the exploratory intervals are uncorrected for multiplicity among *themselves*. The exploratory run produces 93 intervals at the 95% level, and the paper repeatedly argues from "intervals clear of zero", including borderline ones (\kd{kdF} under feature $k$-NN, $+0.018$ with a lower bound of $+0.001$; \kd{hardA} Mahalanobis, $+0.018$ with a lower bound of $+0.004$). At 93 intervals roughly five would be expected to exclude zero by chance.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; a=len(pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv')); s=len(pd.read_csv('results/exploratory/REVISION_FULL3/swaps.csv')); print(a, s, a+s)"  ->  86 + 7 = 93 intervals.  Borderline rows: knnfeat kdF - direct student = +0.017563 (ci_low +0.000648); maha hardA - direct student = +0.018159 (ci_low +0.003961).
- **Suggested fix:** Append one clause: "...and no correction is applied across the 93 exploratory comparisons themselves, so an interval that only just excludes zero should be read accordingly."

### [minor] paper/main.tex line 29 (abstract)
- **Paper says:** "Under the logit-based scores nothing else transfers: distillation beats neither a temperature-scaled direct student nor label smoothing."
- **Problem:** Against label smoothing this reads a compound hypothesis's failure as a flat null. H2 requires distillation to win on detection AND calibration against both controls; \kd{kdA} in fact beats \kd{ls} decisively on the paper's primary detection outcome under both scores, with intervals well clear of zero, and fails only on post-hoc NLL. The body says this precisely ("against label smoothing it trades detection for calibration", contribution 2; "the picture is mixed", Section IV-A), so only the abstract overstates. A reader of the abstract alone would conclude distillation gives no detection benefit over label smoothing, which the confirmatory data contradict.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; c=pd.read_csv('results/analysis/CONFIRMATORY/components.csv'); print(c[(c.analysis=='primary')&c.component.str.contains('ls')][['hypothesis','component','estimate','ci_low','ci_high','p_one_sided']].to_string())"  ->  H2[energy] auroc_kdA_minus_ls = +0.020977 (0.014003, 0.027191), p = 0.000999;  H2[msp] auroc_kdA_minus_ls = +0.006214 (0.003584, 0.008742), p = 0.000999;  the failing component is nll_ls_minus_kdA = -0.039922, p = 1.0.
- **Suggested fix:** "...distillation beats neither a temperature-scaled direct student nor, once calibration is counted, label smoothing" -- or, more directly, "distillation matches a temperature-scaled direct student exactly and only trades detection for calibration against label smoothing."

### [nit] paper/main.tex line 401 (Section V-E, Does the Ensemble Age Faster than Its Members?)
- **Paper says:** "the ensemble ages at $-0.00306$ per week against its members' $-0.00282$, an excess of $0.00024$, and the MSP gap narrows at only $0.00030$ per week."
- **Problem:** Double-rounded. The excess computed from the underlying slopes is 0.000233, which rounds to 0.00023, not 0.00024; 0.00024 is what you get by subtracting the already-rounded table entries. The number carries weight here because the whole paragraph turns on comparing it with the 0.00030 narrowing, and the stated figure makes aggregation look like a slightly larger share of the effect than it is (78% rather than 80%... the direction is against the paper's own caution, but the value is still wrong at the precision stated).
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/drift_slopes.csv'); m=d[d.score=='msp'].set_index('model').slope_per_week; print(m.ensemble, m.member_mean, m.ensemble-m.member_mean)"  ->  -0.0030566, -0.0028238, difference -0.0002328 (i.e. 0.00023).
- **Suggested fix:** Change "an excess of $0.00024$" to "an excess of $0.00023$".

### [nit] paper/main.tex line 374 (Section V-A, final paragraph)
- **Paper says:** "the ensemble-distribution-distillation student, the worst detector in the grid under the energy score ($-0.092$ against \kd{kdA}, CI $-0.100$ to $-0.085$)"
- **Problem:** The lower bound is mis-rounded. The exploratory paired bootstrap gives -0.099485, which rounds to -0.099; -0.100 rounds the wrong way. (Table III's confirmatory version of the same quantity correctly prints $-0.099$, so the two places in the paper now disagree on a number they present as the same interval.)
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; d=pd.read_csv('results/exploratory/REVISION_FULL3/detection_advantage.csv'); print(d[(d.score=='energy')&(d.comparison=='enddA - kdA')][['estimate','ci_low','ci_high']].to_string())"  ->  estimate -0.092009, ci_low -0.099485, ci_high -0.084715.
- **Suggested fix:** Change "CI $-0.100$ to $-0.085$" to "CI $-0.099$ to $-0.085$".

### [nit] paper/main.tex line 283 (caption of Table V, tab:time)
- **Paper says:** "Start date 11: energy AUROC against all test-unknown services, and the student's closed-set macro-F1, by weeks since the end of training. Every second test window is shown."
- **Problem:** The table does not show every second window. Start date 11 has nine windows at 3.5, 7.5, 11.5, 15.5, 19.5, 23.5, 27.5, 31.5 and 35.3 weeks; the table shows the 1st, 2nd, 3rd, 5th, 7th and 9th. Every second window would be five rows (3.5, 11.5, 19.5, 27.5, 35.3), not six. The selection is benign -- the extra 7.5 row is the one just before the crossover -- but the caption misdescribes it, and in a table whose point is where the gap changes sign the rule of selection should be stated correctly.
- **Evidence:** C:/venvs/kd-traffic/Scripts/python.exe -c "import pandas as pd; p=pd.read_csv('results/analysis/CONFIRMATORY/per_window.csv'); v=p[(p.flows=='all')&(p.unknown=='all')&(p.condition=='kdA')&(p.start==11)]; print(sorted(v.weeks_since.round(1)))"  ->  [3.5, 7.5, 11.5, 15.5, 19.5, 23.5, 27.5, 31.5, 35.3].  Table V rows: 3.5, 7.5, 11.5, 19.5, 27.5, 35.3.
- **Suggested fix:** "The first three windows and then every second one are shown."
